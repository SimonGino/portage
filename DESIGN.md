# Portage 管理端设计规范

> 适用范围：M3 React 管理端。现行壳是 **A-v2**（PO 2026-08-16 验收，口径层 v0.75）。本文管视觉、交互原则、信息架构的页面级落点和品牌标记；功能范围与字段语义以 `docs/口径层设计.md`、`docs/MVP设计草案.md` 为准。
>
> 这不是聊天产品。不搬对话框、右对齐气泡、底栏输入槽。

## 0. 一句话定位

这是唯一管理员每天要用的运营工具：**每屏回答一个运营问题，层级靠排版，颜色只给状态与那一抹强调，装饰一律不做。**

## 1. 原则（冲突时按序让步）

1. 事实与安全约束优先：凭证脱敏、错误信息不泄露上游 key / base_url，任何视觉方案不得违反。
2. 高频操作的效率优先于观感：表单少一次点击胜过任何视觉打磨。
3. 层级靠排版（字号 / 字重 / 间距 / 对齐）建立，不靠边框、卡片、色块修补。
4. 颜色只承载语义与强调；强调色单一，且面积 ≤5%。
5. **只做亮色**：暖底羊皮纸是唯一一套表面，不跟随系统深浅色，也不做切换器。

## 2. 壳与导航

- **统一顶栏 + 居中画布列**（v0.54，两空间共用一只壳；推翻 v0.23 的「248px 左栏」——PO 2026-08-31 裁定「一会左右布局一会顶栏布局」必须统一成顶栏。回到顶栏形态但不回 v0.5 的居中 1520）。顶栏 **sticky**：品牌 + 一排 tab + 右侧动作区，`--paper` 底 + `--line-soft` 底线——整窗滚动后滚掉的第一样东西不该是导航。主画布是居中画布列（`--canvas`、上缘圆角 10），**三档宽**：narrow 760（「我的」单列文档流页）/ 默认 920 / wide 1320（调用记录与模型页这种横向吃宽的页；PO 2026-08-31 看流水操作列被削裁定再放宽——流水表 min-width 1100 加 sticky 操作列，1180 装不下）。羊皮纸底照旧：页面底 `#edece3`，画布 `#f5f4ed`。
- **账号动作收进顶栏右侧「头像菜单」**（v0.54，PO 在「头像下拉 / 顶栏平铺 / 导出导入进页面」三选一里拍板）：头像即按钮，点开浮层 = 身份一段（展示名 + 邮箱）+ 修改密码 · 导出配置 · 导入配置 · 退出登录。导出 / 导入仍是全局动作不挂进任何一页（v0.37 的理由不动）；它们的弹框从菜单里开出来，弹框开着时菜单不因外点或 Esc 关闭——菜单一关弹框跟着卸载，流程断在半路。
- **六项**（vNEXT，[#72](https://github.com/SimonGino/portage/issues/72) 用户体系落地加「用户」），顺序即优先级，默认落地「模型」：模型 · API Key · 调用记录 · 接入点 · 排行 · 用户。前五项管「网关转发什么、转得怎么样」，「用户」管「谁能进这个管理端」——它排最后不是最不重要，是最不常开：配一次能用很久。导航选中用浅底药丸 + 字重，不用下划线、不用蓝；tab **纯文字**（v0.54）——左栏时代的六枚线性图标随左栏退役，横排里图标 + 字比纯字更挤，且「我的」空间的 tab 本来就没有图标。
- **去掉概览**。`/overview` 与 `/usage` 重定向到 `/rankings`。**那根时间轴收进排行页**（口径层 v0.86 推翻 v0.75 ② 的后半句）：它不再是一张单独挂导航的图，而是排行页下钻链的第一层，见 §5.2。
- 路由：模型页仍是 `/admin/channels`、`/admin/channels/:id`（文案写「模型」）。
- 渠道清单（搜索 + 列表 + 新建）是模型页**页内左列**（v0.54 从左栏中段搬回页内，sticky 让开顶栏）。其它页不做次级列表，不要硬塞「会话式」侧栏。
- 每页主区：页标题与导航同名（栏目名），不加副标题；页头右是主按钮或工具条，控件同高。**模型页例外**：页头无按钮——获取模型列表 / 手动添加是「纳管模型」区块的动作，不升到页头。

## 3. 排版

- 字体：**衬线只给标题**（`Source Serif 4`，中文落系统宋体 Songti SC / Noto Serif CJK SC，**不加载中文衬线 webfont**），IBM Plex Sans 正文，**IBM Plex Mono 只给标识符**（API Key、模型名、限定名、base_url、状态码、时间戳、token 数）。整句不用等宽。
- 衬线的落点只有四处：页标题、区块标题、渠道身份条上的渠道名（那里它是栏目身份，不是标识符）、空态首句。**表格 / 表单 / 按钮 / 导航一律无衬线**——中文衬线 webfont 是几 MB，不加载就意味着中文衬线只能落各机自带的宋体、逐机不一致，而九列中文流水表在最小那档字号上最赌不起可读性；控件上的衬线还会跟等宽标识符、无衬线数值在同一行里凑成三种字形。
- 字号阶梯 5 档：页标题 26px / 区块标题 / 正文 / 表格与标签 / 辅助说明。不造第 6 档。页标题不再是 40px 超大 display。
- 数字一律 tabular numerals，同列同精度；token 数、耗时不做假精度。
- 界面文案中文，结论先行；术语与 `CONTEXT.md` 一致（接入点 / 候选 / 渠道 / 纳管模型 / 凭证池 / 上游凭证 / API Key）。禁止「供应商」「网关 key」「用量」当页名。

## 4. 色彩与表面

- 灰阶**一律带黄褐暖调，拒绝冷蓝灰**：底、线、井、文字四档同族，没有哪一档可以单独退回常见 SaaS 那套冷灰。
- **唯一主强调色是墨蓝 `#1B365D`**。白名单只有三处：①焦点环（`:focus-visible` 2px 描边）；②文字链接与行内跳转（默认不带下划线，悬停才加）；③页面级合计与主指标那**一个**数（排行页的 token 合计与每行主指标）。**明确不给**：主按钮（仍实心墨色）、导航选中态（仍 `--sunken` + 字重）、开关、Segmented 选中、页码当前页、任何徽章 / 胶囊；表格里的普通数字（耗时、状态码、每行 token）仍是墨色，不许整列刷蓝。自查：截一屏拿取色器估面积，**墨蓝像素超过页面 5% 就是超了**。
- **分类色盘是这条规则唯一的口子，只给排行页，只承载「身份」**（口径层 v0.86）：环形图与占比堆叠条要把同一个模型 / 同一把 key 在两块图里认成同一个东西，那是颜色在编码身份不是编码语义，灰阶做不到。**五个槽，第六个起并进「其余（N）」的中性灰**，不生成第六个色相。**色跟着实体走不跟着名次走**——选中某个区间会让排行重新排序，但每个模型的颜色不变，否则「蓝色那条」在两次点击之间指的不是同一个东西。**每个系列必须直接标名字 + 百分比**：这五个色对羊皮纸底有四个不足 3:1（实测 `#eb6834` 2.9、`#1baf7a` 2.55、`#eda100` 1.96、`#e87ba4` 2.44），只摆色块不成立。**边界**：不进表格数字、不进徽章 / 胶囊、不进状态、不与四个语义色混用；节律带（柱 / 点阵 / 日历）仍是墨色单色五档，热度是量不是身份。色值以 `dataviz` 校验器在 `#f5f4ed` 上跑过的那五个为准，换色必须重跑。
- 语义色仅四个且必须配非颜色线索（图标 / 文字）：成功、警告、错误、信息。四套底 / 线 / 字按暖灰基底调，不用冷调那套——冷调语义色摆到羊皮纸上会发紫。
- 「启用 / 停用」「权重 0 摘除」「凭证停用」是状态，用语义色 + 文字；不给普通数据上色。
- 品牌色图标一律 grayscale（与 §1.4 同一条）。
- 主按钮是实心墨色。页头控件统一 36px / 6px 圆角。模型页「获取模型列表 / 手动添加」是描边组合按钮，落在「纳管模型」标题旁、跟区块级 `.act` 同高（26px），不是实心胶囊、也不占页头。次按钮幽灵。选中态用 `--sunken` + 字重，不用蓝。
- **深度靠边框、极弱阴影、留白**。阴影只给浮起来的东西（对话框、Picker 弹层）；主画布、展开井、渠道身份条、表格一律无阴影。圆角 10 / 8，页头控件与按钮 6。
- 页面是连续画布。区块靠间距分隔。允许的表面：①主画布本身；②浮层（对话框、Picker、头像菜单）——对话框走**弹框档**（v0.47）：标题 14px sans 粗体、头部 14/18 内边距 + 1px 底线，框内主按钮收回 `.btn` 同档（实心墨不变），页面级字号与按钮档不进框；**整框限高 84vh、正文区独立滚动、内容根末尾的动作排钉底**（v0.52）——内容再长头部与签字键也不离屏，正文一律有 18px 内衬（纯文案框走 `.dialog-note`，形制同 `.form`）；③**展开井**（`--sunken`，现存唯一实例是排行页的切片井；「上游设置」的井 v0.33 改弹框，口径层 v0.94 更早把「上游凭证」从井里提出来）；④**渠道身份条**（`--sunken`，模型页，把渠道从模型列表里拆开）。
- 硬性禁止：渐变、发光、毛玻璃、纹理、装饰性阴影、硬阴影、3D、装饰性图表、图标彩色底砖、彩色侧边条、聊天气泡。

## 5. 六页各答一问

| 页 | 问 | 主语 | 主动作 |
|---|---|---|---|
| 模型 | 这个上游纳管了哪些模型？ | 纳管模型列表 | 获取模型列表、启停、复制限定名、协议子集 |
| API Key | 客户端拿哪把钥匙？ | 网关下发的 key | 新建、显示 / 复制、白名单、启停 |
| 调用记录 | 刚才那几次怎么样？ | 逐行流水 | 筛失败、按模型、按端点（筛的是**入站**那条）、详情（request-id / 失败原文） |
| 接入点 | 对外叫什么名字？ | 别名 | 新建、绑候选。直连用限定名即可，不必建 |
| 排行 | 什么时候烧的 → 那一段是怎么构成的 → 谁在烧 | 区间 + 名次列表 | 1 / 7 / 30 天；点区间下钻；按模型 / 按 API Key / 按用户 / 按上游凭证 |
| 用户 | 谁能进来、怎么进来？ | 用户列表 | 添加用户、任免 / 停用、邀请码生成 / 撤销、登录与邮件配置 |

### 5.1 模型页

渠道列表在页内左列（v0.54 从左栏中段搬回页内；sticky 让开顶栏，两栏之间的竖线画在右栏左边框上——v0.16 ① 的理由原样）：名字 + 图标 + 「缺凭证 / 无协议 / 停用」。不重复协议集、模型数、凭证计数。排序（v0.50）：**停用的沉底，其余按 id（接入先后）**——组内都按 id，稳定不随改名跳位。

主画布第一眼是**这个渠道的纳管模型**。H1 与导航同名（「模型」），不加副标题。渠道是 sunken 身份条（衬线 + 小字「渠道」），身份条之下依次是「API 地址」「上游凭证」两个**常驻展开的区块**与等宽模型列表（各配同一档小字标题，如「纳管模型 · N」——三段是同一层：都是这个渠道名下的清单项。口径层 v0.94，信息架构参照 Cherry Studio 服务商页，视觉与命名不跟；v0.33 对调两区块，地址在上——地址定义渠道是谁、声明了哪些协议，凭证是从属物，接一家上游也是先填地址再贴 key；v0.35 回到常驻展开，推翻 v0.34 ① 的默认收起——管理、检测、每协议的预览地址一眼可见，折一层就是多一步）。这一页的行内动作走**两级动作按钮**（v0.38，推翻 v0.34 ② 的墨蓝文字链接）：区块级动作（上游设置、编辑 / 完成、管理、检测、批量粘贴）是 `.act` 描边小按钮（笔画图标 + 文字，落在区块标题行），值旁动作（显示 / 隐藏、复制、删行）是 `.act-icon` 无边框图标微钮，贴着值站。凭证行只摆正在被用的那一把（掩码 + 显示 / 复制微钮 + 「共 N 把在轮」），「管理」「检测」在「上游凭证 · N」标题旁，池子的逐条 CRUD 在「管理」弹框里；删除（或停用）启用渠道的最后一把启用凭证不拦：后果写进确认键（「删除并停用渠道？」/「连渠道一起停用？」），确认后先停用渠道再动凭证（v0.35，写后校验不放宽）；「检测」点开**检测弹层**（口径层 v0.96）：预选当前凭证，选全部或单个模型、勾协议（默认全勾已声明协议，v0.51 推翻 v0.32 ① 的默认 openai），结果就地显示为模型 × 协议矩阵（✓ / ✗ / ?，摘要摆屏幕不进 tooltip），关弹层即失——「管理」弹框每把凭证行也有检测，开同一个弹层、预选那一把；页面上没有常驻探测区块，模型行不放检测按钮。API 地址区块默认**只读**：每协议一行预览地址（网关实际会请求的完整地址），标题旁「编辑」原地切换编辑态（v0.35）；编辑态是**共用前缀 + 协议勾选**（v0.46，推翻 v0.32 ③/v0.96 的每协议一行——「同一上游共用前缀就几行填同一个值」的抄写苦役是它要消掉的）：一份共用前缀输入 + 三颗协议 chips，勾了即声明（把该协议的地址写进 map；声明判据仍是「哪些协议填了地址」，勾选只是写 map 的手势，不落库成独立字段），已声明协议各一行预览紧随其下，行尾「单独填」微钮给个别协议换自己的前缀（覆盖行带「单独填」标记，可一键恢复共用），失焦或回车即存，至少保留一个声明协议（会把声明集清空的动作不给口），「完成」只收面板不提交。新建渠道表单端点设置用同一控件（BaseURLFields），不再折叠（v0.32 ③ 折的是每协议一行的地址框；chips 一行三颗，没有可折的体积）。渠道名旁启停立刻生效。「上游设置」（改名、并发、能力位、删除渠道）是身份条右侧的 `.act` 描边按钮（v0.38），点开**弹框**（v0.33，推翻「展开井」）：保存沉右下实心墨，删除渠道 ghost 起步坐保存对面，有未保存改动时遮罩点击不关（Esc 照关）。获取模型列表与手动添加是「纳管模型 · N」标题旁的描边组合按钮（v0.39，推翻 v0.25 的页头位——它是这块清单的动作，跟「管理」「检测」同一档位置），添加条默认收起。新建渠道时主区整页是表单，没有模型列表。

模型行：供应商图标 + 裸名 + 限定名（可复制，停用的不给复制）+ 协议子集 + 输入上限（v0.36）+ 定价（v0.41）+ 启停 + 删除（二次确认）。输入上限与协议子集同层同形，三态同一副胶囊身形（v0.38）：未设是虚线胶囊「+ 上限」，设了是实底芯片「上限 ~N」悬停浮出铅笔；编辑是胶囊输入组（数字与「token」单位同框，聚焦整框点亮，失焦或回车即存，空/0 = 清成不限，支持 `200k` / `1m` 缩写），文案带「估算」不承诺精确（口径层 v0.99）。

定价（口径层 §2.10，[#74](https://github.com/SimonGino/portage/issues/74)）沿用同一副胶囊三态（v0.41）：未定价是虚线胶囊「+ 定价」，定了是实底芯片「$入/$出」悬停浮出铅笔（缓存两价小字随行，四价全文进 title）；编辑是胶囊输入组四价各一框、共用「$/M」单位，焦点离开整组或回车即存，Esc 丢弃——**空 = 清回未定价（null），0 = 真免费**，两态不抹。「未定价」提醒是 `tag-warn`，判据 = 四价全 null **且有用量**：没人用过的条目不催。渠道在「上游设置」里标注了 models.dev 的 provider 时，模型行给 `chip-suggest` 建议价（「models.dev $x/$y · 采纳」，同协议子集那颗「采纳」的形制）：只提示，点了才落库，快照缺哪价就建议 null 不补 0。单价显示用定价惯用形（$3、$0.075），不套 §12 的金额两位——那条管算出来的钱，单价抹两位会把 $0.075 写成 $0.08。

### 5.2 API Key / 接入点 / 调用记录 / 排行

后三项维持扁平表或排行列表，不套主从两栏。

- 调用记录：定高视口、九列、每页 10 条、右下可跳页码。详情判据是「这一行有没有细节可看」（`status >= 400 || request-id 非空 || 排队耗时 > 0 || cost 非 null`，第四项 v0.41）。详情框的「成本」一格判 cost 非 null（null = 无用量可计，不摆「$0.0000」空壳；0 = 有用量但未定价或真免费，照摆），按 §12 明细档给四位小数。上游原文只在失败行。request-id 可复制，空为 —。排队耗时只在真排过队（> 0）的行摆，不摆「0ms」空壳。「端点」列摆**入站 → 出站两条真实路径**（都去 `/v1/` 前缀，全路径进 title）：主行入站，副行出站带箭头。协议一概不摆在这一列——入站协议由路径完全决定，出站协议推不出出站路径（`count_tokens` 没有出口对应物）；要看协议去详情框。只摆路径不摆 base_url。回落两档靠入站那格分辨：两格皆空是老流水，退回「入站 / 上游 <协议>」原显示；入站有而出站空说明这次没发到上游（401 / 429 / 501 / 队满），副行「—」。筛选三个 Picker（模型、端点、用户）+ 一个 Segmented（只看失败），端点筛的仍只有入站那条；用户 Picker（v0.49，#75）只在库里多于一人时出现——单人库里它恒只有一个可选项，摆着只占宽度；选项来自用户列表不从流水聚合（没烧过的用户也该筛得到，筛出空表本身就是答案），无主行不属于任何人、筛不到。「API Key」列在 key 名下压**归属副行**（属主邮箱，LEFT JOIN 现查；无主 key、未鉴权与回填前的老行不摆——空行不解释）。
- 排行：**三层下钻，同一屏从上往下读**（口径层 v0.86）。①**节律带**答「什么时候烧的」，一格 = 一个区间，三档窗口三种排布：1 天 = 24 根柱，7 天 = 7 行 × 24 列点阵（尺寸 + 深浅双编码，小屏上深浅先糊、尺寸还在），30 天 = 窗口盖住的那六周日历。**未到的区间不画、不可点、不进活跃分母**（自然日窗口，口径层 v0.55）。热度是墨色单色五档，分位切档不是把最大值等分。②**切片井**：点一格展开，答「那一段是怎么构成的」——缓存 / 净输入 / 输出三段，不列 reasoning。再点一次或 Esc 收起。③**排行列表**：名次 + 图标 + 名字 + 一个主指标（token）+ 占比，**跟着选中的区间走**；没选就是整窗。选中后三层必须同一个 scope。④环形图（不带图例）与「按模型 / 按 API Key」两条占比堆叠条摆在中间，用 §4 那套分类色盘；名字与百分比只由堆叠条给一次。⑤五个统计量一行：合计 / 每区间均值 / 峰值区间 / 活跃区间 N/M / 最长连续——**这一行不是「一排等大方块」**（§9），它是同一个数组的五个读数，无框无底、字号有主次。⑥只留一个合计当占比分母。不做热门榜、厂商份额、环比、费用、逐模型曲线。不给排行的每一行画占比条（占比条是堆叠条那两条的活）。⑦维度加「按用户」（v0.49，#75）：标签是邮箱，无主两档分开——无主 key 的真调用归「(无主 key)」、压根没鉴权归「(未鉴权)」，混成一档会把被扫的 401 算进无主的账；环与堆叠条照旧恒按模型 + API Key，不跟维度换。「按 API Key」的行附注里带归属邮箱——key 名自 #73 起按用户唯一而非全局唯一，跨用户撞名靠它分辨。
- 接入点空态两句：没渠道先去模型页；有渠道没接入点则写「不必建，客户端可填限定名」。

### 5.3 用户页（vNEXT，#72；信息架构级）

三张卡纵排，扁平表不套主从，同答「谁能进来、怎么进来」——用户是结果，邀请码与登录配置是入口，三者是一条链：

- **用户列表 + 「添加用户」**：邮箱、名字、角色、登录方式（密码 / 仅 OAuth）、**月配额**（v0.49，#75）、状态（正常 / 待验证邮箱 / 已停用）、创建时间；行内动作是任免（设为管理员 / 降为用户）与停用 / 启用，都过二次确认。月配额那格是幽灵按钮（「不限」/「封停」/ `$X.XX/月`），点开调额弹框：留空 = 不限额（null），0 = 封停并出 `bar-warn` 明说「这个账号的转发请求会立即全部被拒」——0 与 null 是两个意思，输入框上就得分清；负数不给存。调额每请求生效、当月已用不重算（口径层 §2.10）——任免与冻结每请求生效，没有「保存前反悔」的缓冲。**最后一个启用的 admin 不许降级、不许停用**（自己也算），护栏在服务端，被拦的 400 原文出页顶 ErrorBar。「添加用户」是 SMTP 未配时的发号逃生门：admin 面对面发号，号一出生即已验证。
- **邀请码**：批量生成（可选有效期，小时粒度）、复制、撤销；已用的码不可撤——它只剩「谁用的」这条记录，摆在状态列。
- **登录与邮件**：站点外部 URL、SMTP（含测试发信）、GitHub / Google client 与拼好待复制的回调地址。**secret 三样只显示「设没设」，永不回显**（同「上游 key 只存服务端」口径）；secret 输入框留空 = 保留现值，清空是显式动作。

声明文件形态下用户体系整体不挂载（#66 互斥），本页连同注册 / 找回等门外页一并不存在。门外页（登录 / 注册 / 找回 / 重置 / 验证 / 完成注册）共用登录页那张纸——同一段旅程不换房间。

## 6. 图标：两套逻辑，不许互为兜底

跟 Cherry Studio 同一条：**预置官方渠道有图，自定义中转走首字母；模型名另表按族认。**

- **渠道**（`CHANNEL_HOSTS` / `CHANNEL_NAMES`）：只认官方那几家（Anthropic / OpenAI / 百炼 / Vertex / DeepSeek / Kimi / 智谱等）。host 写死官方后缀，不写会把 PAI 认成百炼的宽模式。名字只认人明显按官方起的（`ali-prod`、`kimi`）。对不上就是首字母。
- **模型**（`MODEL_PATTERNS`）：只看模型名 / 限定名，按族（`gpt` / `claude` / `qwen` / `glm` / `kimi`…）。跟挂在哪条渠道无关。
- 只有供应商一级的方块，不做逐模型图标（两套视觉会打架，还得跟着上游新模型一直追）。
- 资产在 `web/src/icons/`。加图标见该目录 README。

## 7. 表格与表单

- 语义化 `<table>`。文本列左对齐，数值列右对齐。调用日志紧凑密度，配置类普通密度。
- 凭证默认掩码，给「显示」「复制」。列表与错误回显不出现完整上游凭证 / base_url（转发链路仍永远不带；管理端凭证池按口径层 v0.47 可回读）。
- 停用的 key、weight=0 的候选、停用的渠道：整行降灰 + 状态标注，不隐藏。
- 下拉一律 `Picker`，全站不留原生 `<select>`。
- 批量要先划范围：获取模型列表的全选只作用于当前筛出来的那些。
- 开关就该是开关：启停立刻生效，不做成「复选框 + 保存」。
- 能拼出来的结果就摆出来：Base URL 下逐协议给出完整地址预览。
- 已经存在的东西不要长成待填的表单（凭证名 ghost，添加行才是描边框）。
- 一行里谁是主语，谁就吃剩余宽度。
- 校验失败就地报错并保留输入。删除渠道 / 接入点 / key 二次确认并写明影响面。

## 8. 动效

默认静止。动效只用于：保存成功、加载中、危险操作的撤销倒计时。无滚动入场、无 hover 位移。尊重 reduced-motion。

## 9. 反模式清单（出现即打回）

- 聊天气泡、底栏「Ask anything」、把探测画成助手回复。
- 全大写小标题 / 装饰性编号。
- 居中大 hero + 卡片栅格的官网式首屏。
- 指标堆砌成一排等大方块。
- 徽章 / 胶囊标注普通元数据；图标当装饰。
- 每个区块一张卡、卡里套卡、用边框修层级。
- 无意义颜色；图表凑数（一个数值画一根柱）。

## 10. 验收自查

1. 打开管理端，能否立刻看见「纳管了哪些模型」而不是一张渠道设置长表？
2. 中转渠道是不是首字母、官方渠道和 `gpt-*` / `claude-*` 是不是有图？
3. 新增一个渠道（含纳管 3 个模型 + 2 份凭证）能否 2 分钟内完成？
4. 全站搜不出一个完整上游凭证出现在不该出现的地方（列表、流水、错误回显）？
5. 删掉任意一个边框 / 卡片 / 颜色，层级是否依然成立？成立就删。井是例外：收起之后列表自己成立。（这一条禁的是拿边框修层级，不是禁边框——真实分组处的界照旧合法，判据见 v0.16。）
6. 截一屏拿取色器估面积，墨蓝是否还在 5% 以内？超了就先退掉离白名单最远的那处（典型是整列刷蓝的数字）。

## 11. 品牌标记

主标记是「hull off the water」：船体离水，下面两段水道被切断，货从缺口上面过去。**缺口是主语**。24×24 网格、3px 模数、一条 path：

```
M3,6 L21,6 L17,13 L7,13 Z  M0,17 L9,17 L9,21 L0,21 Z  M15,17 L24,17 L24,21 L15,21 Z
```

§4 的硬性禁止对标记同样成立，颜色不给标记用。

**两份资产，不是一份**：

- `web/src/brand.tsx` —— 页面内用，`fill="currentColor"`，跟着 `--ink` 走。
- `web/public/portage-mark-v2.svg` —— favicon，两套颜色写进 SVG 自己的 `<style>`（亮 `#1c1a15` / 暗 `#f0ede2`），由 `prefers-color-scheme` 挑。**§1.5「只做亮色」管不到这里**：favicon 是浏览器单独加载的独立文档，摆在**标签栏**上，标签栏该不该是暗的跟管理端页面用不用暗色主题是两件事——`#1c1a15` 那枚摆到暗色标签栏上就是一团黑。这里的 `prefers-color-scheme` 保留。

两处 path 必须逐字一致。favicon 路径写死在 `/admin` 下；改标记要顺手改文件名（`serveSPA` 对非 html 发一年 immutable）——v0.28 换色是这条的第一次实例：`portage-mark.svg` 改名 `portage-mark-v2.svg`，旧文件删掉，`web/index.html` 的 `<link rel="icon">` 跟着改，`path` 与 `brand.tsx` 的 `MARK_PATH` 一个字不动。

**16×16 是硬底线**。未做：wordmark；32px PNG 兜底。

资产出自 Claude Design 项目「Portage Logo Design Brief」（issue portage-legacy#46），不含任何第三方品牌资产。

## 12. 用户面板：「管理 ⇄ 我的」两空间（v0.40，[#67](https://github.com/SimonGino/portage/issues/67) 拍板）

多用户体系（口径层 §2.10）的页面落点。**单应用单登录**，同一挂载闸；原型三变体实评后 PO 取 **B+C 合成**——结构取「双空间切换」，「我的」空间形态取「独立入口」的轻面板。

- **两空间**：管理空间 = 本文档 §2~§11 的现有形态；「我的」空间是同一应用内的另一组 tab。普通用户登录后直接落入「我的」，**永远见不到管理导航**；admin 两边都有，切换入口统一在顶栏右侧的幽灵按钮（管理空间给「我的」、「我的」给「管理」；v0.54 前管理侧是左栏顶部的分段控件，随左栏退役）。
- **「我的」空间壳**：与管理空间**共用同一只顶栏壳**（v0.54，推翻本条原「无左栏轻面板 vs 左栏控制台」的双壳并立）：tab 换成本人五页，右侧配额微条 + 头像菜单（菜单只有退出——改密码、邮箱、OAuth 都住在「账号」页，不摆第二个入口）；主区**单列文档流**（narrow 档 760px 居中；「调用记录」「用量与配额」两页放宽到 wide 档（1320px，v0.53 时 1180）——流水表十来列、排行是环 + 节律带两栏，窄列里挤不开，v0.53），卡片纵排。视觉沿用 §3/§4 的排版与色彩纪律，「产品感」多于「控制台感」——没有密表格压满屏，配额永远置顶。
- **五页**（v0.53 拆流水，推翻 v0.40 的四页）：我的 Key / 调用记录 / 用量与配额 / 模型 / 账号。我的 Key = 配额卡 + key 表（名字、明文可复制、白名单、启停；早期无明文的 key 如实说明）；调用记录 = §5.3 那张全功能流水表的**本人形态**（`logs/view.tsx` 公共组件，路由 `/my/logs`）——定高视口、翻页、模型/端点/只看失败三筛照抄，裁掉上游凭证列、用户筛选与归属副行（口径层 §2.10 的运营细节），详情走本节下方那条两项判据；用量与配额 = 本月配额进度 + §5.2 那条下钻链的**本人形态**（`rankings/view.tsx` 公共组件：节律带 → 构成 → 谁在烧），维度只剩按模型/按 API Key，排行附注多一截费用（配额按钱记账，「哪个模型烧了我多少钱」得答得上来；管理端排行不跟）——v0.48 的聚合表与简版流水退役（聚合表被排行列表顶掉，同一份数据不画两遍）；模型 = 全量开放的可用模型清单（只读）；账号 = 邮箱/展示名/改密码/OAuth 绑定解绑。
- **金额展示**：一律 `$X.XX` 两位小数，不足一分显 `<$0.01`，流水明细展开给四位；不做多币种。
- **URL（v0.48）**：整站前缀由 `/admin` 换名 `/panel`（口径层 v1.05，[#76](https://github.com/SimonGino/portage/issues/76) 三裁定；旧前缀 GET 302），「我的」五页路由 = `/my` / `/my/logs` / `/my/usage` / `/my/models` / `/my/account`（`/my/logs` v0.53 加）。OAuth 绑定从「账号」页发起（`?mode=link`），回执由 302 带 `oauth_linked` / `oauth_error` 落回「账号」页顶部的 bar。
- **本人流水的详情**（v0.48）：判据只剩两项（`status >= 400 || cost 非 null`）——request-id 与排队耗时是运营细节，用户侧那两档整个不存在；详情框走弹框档（`Dialog` + `log-meta`），摆模型→上游、渠道名、状态 + 错误词、成本（明细四位）与上游错误原文，凭证名与 request-id 不出现。
- 未验证邮箱的用户登录后只见「去验证」页，四页不挂（口径层 §2.10 功能全锁）。
- 原型留档：分支 `prototype/panel-ia-67`（三变体 + 切换条，DEV-only），是历史原型不作实现依据（同 design-demos 成例）。

## 版本记录

正文以本节之上为准。下面只记口径变化与依据，不记流水账。修改人一律 `jinpenga`。

| 版本 | 日期 | 修改人 | 说明 |
|---|---|---|---|
| v0.54 | 2026-08-31 | jinpenga | **两空间统一成顶栏壳，左栏退役**（§2 前两条、§5.1、§12；PO 2026-08-31「一会左右布局一会顶栏布局让我很不爽，我要统一改成顶栏」——推翻 v0.23 的 248px 左栏与 v0.40「双壳并立」，此前 v0.53 问答里「保留两种布局、对齐细节」的建议被此裁定取代）。①一只 `TopShell` 两空间共用：sticky 顶栏（品牌 + tab 排 + 右侧动作区）+ 居中画布列三档宽（760/920/1320；管理端调用记录与模型页走 wide，其余 920。wide 档初值取了「我的」的 1180，比左栏时代流水内容列的 1240 还窄，操作列被削——PO 同日裁定放宽，定 1320）；②左栏底部四动作（修改密码 / 导出配置 / 导入配置 / 退出登录）收进顶栏右侧**头像菜单**（PO 在「头像下拉 / 顶栏平铺 / 导出导入进页面」三选一里拍板头像下拉）——导出 / 导入的弹框从菜单里开出，弹框开着时菜单不因外点或 Esc 关闭；③模型页渠道清单从左栏中段搬回**页内左列**（sticky 让开顶栏，右栏左边框画竖线，v0.16 ① 复用），复活并重写了 v0.23 时代遗下的 `.split`/`.master` 死样式；④管理端 tab 去图标（横排里图标 + 字更挤，「我的」tab 本来无图标）；⑤空间切换统一成顶栏右侧幽灵按钮，左栏顶部的分段控件随左栏退役；⑥流水定高视口的固定开销按新壳重量（190 → 235：sticky 顶栏 53 进账，`.main` 内滚改整窗滚） |
| v0.53 | 2026-08-31 | jinpenga | **「我的」空间调用记录与用量升级成管理端两页的本人形态**（§12；PO 2026-08-31 嫌用户侧「丑丑的样子」，在「公共组件升级 / 整页搬家砍 admin / 只页内换装」三选一里拍板公共组件，tab 结构在「五 tab 拆流水 / 六 tab 排行独立 / 四 tab 页内升级」里拍板五 tab，admin 导航六项不动）。①`Logs.tsx` / `Rankings.tsx` 实体迁 `logs/view.tsx` / `rankings/view.tsx`，带 `mine` 形态：数据换走 `/my/logs` / `/my/usage`（+ 新端点 `/my/usage/buckets`，归属焊死在服务端 WHERE），表与图的形状两边一致；用户侧裁的全是口径层 §2.10 圈定的运营细节——上游凭证列、用户筛选与归属副行、按用户/按上游凭证两维度、详情里的 request-id 与排队；排行附注在 mine 侧多一截费用（配额按钱记账，管理端不跟——那页形态多轮拍板过不顺手动）。②「我的」四 tab 变五：流水从「用量与配额」页尾拆出独立成「调用记录」tab，「用量与配额」原地升级成配额卡 + 排行下钻链，v0.48 的聚合表与简版流水退役（同一份数据不画两遍）。③这两页主区放宽 1180px，其余页 760px 照旧 |
| v0.52 | 2026-08-31 | jinpenga | **弹框整框限高、正文区滚**（§2 表面②；PO 2026-08-31 看导入试算 23 行清单把框撑到近满屏、按钮沉到屏幕边缘后，在「清单内滚 / 整框限高 / 不动」三选一里拍板整框限高）。①Dialog 壳 `max-height: 84vh`（与遮罩上下各 8vh 内衬凑满一屏），新增 `.dialog-body` 正文滚动区，头部钉住——内容决定高度就是 v0.19「按钮跟着内容跑」那个病在弹框上的再现；②内容根**末尾**的 `.form-actions` 以 sticky 钉底（`:last-child` 判据把检测弹层那颗「检测」排除在外——它身后挂着结果区，是表单中段动作不是页脚），负外边距把内容根底衬挪进按钮排，不滚动时布局分毫不变；③导入配置 / 导入完成 / 导出失败三框补 `.dialog-note` 内容根（形制同 `.form`，段落 / 警示条 / 清单的自带外边距交给 gap）——它们此前是全站唯一正文贴框边的弹框，v0.47 收壳没收到这一处 |
| v0.51 | 2026-08-31 | jinpenga | **检测弹层协议默认全勾**（§5.1 检测弹层句；口径层 v1.06，PO 2026-08-31 看截图裁定「默认都勾选协议」）。**推翻 v0.32 ① 的默认只勾 openai**：默认全勾已声明协议，勾选顺序仍按回退序排列；可选项 = 已声明协议、勾选不落库、矩阵形态与三态措辞无一处动 |
| v0.50 | 2026-08-31 | jinpenga | **左栏渠道列表排序：停用沉底、其余按 id**（§5.1；PO 2026-08-31 口头）。此前按接口原序（裸 id 序），停用项混在中间，天天扫的清单每次都要跳读；组内不按名字按 id，是让顺序稳定——改名不跳位，新接的永远长在组尾。默认选中随之落在第一个未停用的渠道上 |
| v0.49 | 2026-08-31 | jinpenga | **配额与用户维度落进管理端**（§5 表、§5.2、§5.3；[#75](https://github.com/SimonGino/portage/issues/75)，口径层 §2.10 配额/流水两条的页面落点）。①用户页加「月配额」列：幽灵按钮（「不限」/「封停」/ `$X.XX/月`）开调额弹框，留空 = 不限（null）、0 = 封停出 `bar-warn` 直说后果、负数不给存——null 与 0 是两个语义（默认不限 vs 立即全拒），输入框上就得分清，placeholder 写「不限额」而不是 0。②调用记录「API Key」列压归属副行（属主邮箱；无主/未鉴权/老行不摆），筛选加用户 Picker——只在库里多于一人时出现（单人库里恒只有一个可选项，摆着只占宽度），选项来自用户列表不从流水聚合（没烧过的用户筛出空表，那本身是答案）。③排行维度加「按用户」（标签邮箱；「(无主 key)」与「(未鉴权)」两档分开——混了会把被扫的 401 算进无主的账）；「按 API Key」行附注带归属（key 名 #73 起按用户唯一，跨用户撞名靠它分辨）。环与堆叠条恒按模型 + API Key 不动（v0.86 ⑧ 原样） |
| v0.48 | 2026-08-31 | jinpenga | **§12「我的」空间落地 + 整站前缀换名 `/panel`**（§12 三条补写；[#76](https://github.com/SimonGino/portage/issues/76)，口径层 v1.05 的页面落点）。①四页照 v0.40 形态落地：顶栏 = 品牌 + 四 tab + 配额微条（`quota-chip`：不限显本月已花、超限/封停转 err 色）+ 头像，主区单列 760px；配额卡置顶于「我的 Key」与「用量与配额」两页（不限额时大数字 + 「不限额」，设限走进度条、超限转警示色、封停直接 `bar-error`）。②URL：整站 `/panel`（单 basename；`/admin` 只留 GET 302，非 GET 404），「我的」四页 = `/my` 起头四条；OAuth 绑定 `?mode=link` 发起、302 带 `oauth_linked` / `oauth_error` 回「账号」页。③本人流水详情判据收成两项（`status >= 400 || cost 非 null`）——request-id 与排队耗时在用户侧整个不存在（运营细节不对用户展示，口径层 §2.10），详情走弹框档 `Dialog` + `log-meta`；「修改密码」弹框标题去「管理」二字（普通用户也用它，"管理密码"名不副实）。管理空间 §2~§11 无一处动 |
| v0.47 | 2026-08-30 | jinpenga | **弹框壳降档：标题、头部、主按钮收回弹框规格**（§2 表面②；PO 2026-08-30 贴「发测试邮件」弹框截图裁定「每次这种弹框都是这样子，需要改动」——同病见 v0.19 那句「都不协调，大小宽度等」）。三处：①标题从 22px 衬线（渠道身份条同档）降回 14px sans 粗体——页面级字号进了 520px 的小框，标题比正文大两档，头重；②头部 padding 22/22/8 + 无底线改回 14/18 + 1px 底线——与表头、区块头同一条分隔语言；③弹框内 `.btn-primary` 收回 `.btn` 同档（13px / 6 12，实心墨不变）——主次按钮并排差一整档的失衡只在框里成立，门外页与页头 CTA 的页面档不受影响（作用域挂 `.dialog`）。**配套**：AGENTS.md 硬约束加一条「管理端弹框走弹框档」——新弹框一律走 `Dialog`/`Confirm` 组件、规格以本条为准，文案直说动作与后果 |
| v0.46 | 2026-08-30 | jinpenga | **API 地址改「共用前缀 + 协议勾选」**（§5.1；口径层 v1.04；PO 2026-08-30 裁决方案 A，起因是「我都没协议转换，模型端点协议都写两份这是何必呢」）。**推翻 v0.32 ③/v0.96 时代的每协议一行**：一份共用前缀输入 + 三颗协议 chips，勾了即声明；个别协议要不同前缀的降级为预览行上的「单独填」覆盖（带「单独填」标记、可恢复共用）；新建表单与编辑态收口成同一个控件（BaseURLFields），「更多设置」折叠随地址框一起退役。**数据层一字未动**：落库的仍是「协议 → 出站根地址」map，声明判据仍是「哪些协议填了地址」（v0.96 ②），勾选只是写 map 的 UI 手势、不是独立落库字段——存量渠道无损（splitBaseURLs 按回退序第一个已填协议的值拆共用前缀，值不同的算覆盖，join 回去不变）；至少一个协议的闸照旧（会把声明集清空的勾/恢复在 UI 里不给口，服务端 Validate 同闸兑底） |
| v0.45 | 2026-08-30 | jinpenga | **区块标题行动作统一右置**（§5.1 标题行；PO 2026-08-30 口头：「编辑、管理/检测、获取模型列表+这些统一放在所在行的右侧」，推翻 v0.38/v0.39 以来「跟在标题后」的左聚簇）。实施不动任何按钮形制与归属，只挪水平位置：`DetailBlock` 头部给动作套 `.detail-block-acts` 容器（`margin-left: auto` 只落一次；成对动作「管理 / 检测」的贴近规则随容器迁到 `.detail-block-acts .act + .act`），标题留左、动作整组靠右。立论：与渠道身份条上的「上游设置」同一条行节奏——名词在左、动词在行尾等手，标题旁「多了什么」不再需要逐行扫；三个落点（地址「编辑」、凭证「管理 / 检测」、模型「获取模型列表 +」）自此与身份条动词同缘起对齐 |
| v0.44 | 2026-08-30 | jinpenga | **baseurl 预览行加复制微钮 + 「+ 上限」「+ 定价」并排一行**（PO 2026-08-30 在渠道页截图上两处直接批注）。①**预览地址行尾加 `.act-icon` 复制钮**（v0.38 ② 「值旁动作」成例，凭证行同款；点了抄完整预览地址，成功原地变 ✓ 绿一拍）——这串地址正是要抄进客户端配置的东西，手抄一个长域名必错，同块下方凭证行早已是值旁微钮，同一行节奏里不该一边能点一边只能选中拖蓝。复制逻辑收口成 `ui.CopyIconButton`（clipboard 失败不报错，值本身在页面上可选；SecretValue 的「失败改亮明文」兼底语义不同，不并入）。②**模型行的上限与定价两颗芯片并排一行**（推翻此前各自占一整行）：两颗收起态都是芯片，叠两行白耗一倍高度——模型行是网格里最高频扫视的单元，高度就是可读性。各自仍是独立组件（编辑态原地展开），新容器 `.model-meta` 只负责排成一行、接住内层整行占位，四价输入组展开时放不下折行。无字段与语义变化，不涉口径层 |
| v0.43 | 2026-08-30 | jinpenga | **导入确认框从说教改对账单**（口径层 v1.03 试算接口的页面落点；PO 2026-08-30 看确认框截图裁定「过于抽象」后拍板 dry-run 方向）。**推翻 v0.37 的「先警告后清单」时序**：选完文件立即试算，确认框直接摆试算结果——「试算 {文件名}：将新增 N 项、删除 M 项」汇总行 + `.import-changes` 逐行清单，无变化明说「配置无变化」；试算 400 的原文 pre-wrap 原样显示且**确认键整个收起**（试算不过真导入必不过，摆着确认键就是邀请人去撞闸），真导入失败的 400 原文同位显示。覆盖语义压成常驻一句「文件里没有的一律删除，有的一律按文件覆盖」，v0.37 那段事务性说教（「整份回滚、一次报全、不留半截」）退役——失败时 400 原文自会说话。**`Confirm` 补 `danger` 形态**：未举起即实心红——全量覆盖是弹框里唯一主操作，第一眼就该危险；两段式确认的防误触语义不变。成功弹框缩成一句（清单确认阶段已看过，同一份数据不摆两遍），关框整页重载照旧 |
| v0.42 | 2026-08-30 | jinpenga | **导航加第 6 项「用户」，用户页信息架构定形**（§2、§5 表、§5.3 新节；[#72](https://github.com/SimonGino/portage/issues/72) 用户体系落地，PO 裁定补任免 / 停用后一并落档）。§2「五项」改「六项」，「用户」排末位（最不常开，配一次用很久）；§5 标题随之「五页」改「六页」。用户页三卡纵排（用户列表 + 添加用户逃生门 / 邀请码 / 登录与邮件配置），行内任免与停用过二次确认，最后一个启用的 admin 不许降级停用（护栏在服务端）；secret 永不回显、留空 = 保留。门外页（注册 / 找回 / 重置 / 验证 / 完成注册）共用登录卡。§12 两空间不受影响——本页是**管理空间**的治理面，普通用户看不见 |
| v0.41 | 2026-08-30 | jinpenga | **计价落进管理端**（[#74](https://github.com/SimonGino/portage/issues/74)，口径层 §2.10）。①模型行加「定价」，沿用 v0.38 胶囊三态（虚线「+ 定价」/ 实底「$入/$出」浮铅笔 / 四价胶囊输入组共用「$/M」），空 = null（未定价）、0 = 真免费；「未定价」`tag-warn` 判据 = 四价全 null 且有用量；provider 标注（「上游设置」新增 Picker「厂商标注」，未标注是合法常态）解锁 `chip-suggest` 建议价，只提示、点采纳才落库。②调用记录详情判据加第四项 `cost 非 null`，详情框「成本」四位小数、null 不摆空壳。③单价显示用定价惯用形不套 §12 两位——§12 管算出来的钱，单价抹两位会把 $0.075 写成 $0.08 |
| v0.40 | 2026-08-30 | jinpenga | **用户面板信息架构定稿：「管理 ⇄ 我的」两空间**（§12 新节；wayfinder [#67](https://github.com/SimonGino/portage/issues/67)，PO 于三变体原型实评后拍板 B+C 合成）。结构取「双空间切换」（单应用单登录，admin 双侧可切）、「我的」空间形态取「独立入口」的轻面板（无左栏、顶栏四 tab + 单列文档流、配额置顶）；普通用户永不见左栏；四页 = 我的 Key / 用量与配额 / 模型 / 账号；金额一律 `$X.XX`（不足一分 `<$0.01`，明细四位）。管理空间 §2~§11 原样不动。原型留档 `prototype/panel-ia-67` 分支，历史原型不作实现依据。口径与字段边界见口径层 §2.10 |
| v0.39 | 2026-08-28 | jinpenga | **获取模型列表 / 手动添加从页头挪到「纳管模型」标题旁**（§2、§4、§5.1；PO 2026-08-28 口头：「放到下面纳管模型那个位置右边，而不是放在最上面」）。**推翻 v0.25 的页头位**：组合按钮形状不动（左拉列表、右「+」手动添加，一体描边不是实心胶囊），落点改成跟「管理」「检测」同一档——区块标题行。页头从此只剩栏目名「模型」，与空态、新建渠道两态同形。高度收到 26px 对齐 `.act`（不再是页头那档 36px）：它已经不是页面级动作，撑到页头尺寸会把这一行抬得比上面两块标题行高一截。添加条仍默认收起，展开后落在标题下、列表上 |
| v0.38 | 2026-08-28 | jinpenga | **行内动作换装：文字链接 → 两级动作按钮**（§5.1；PO 2026-08-28 指着截图裁定「太质朴了，重新设计按钮和交互」）。**推翻 v0.34 ② 的墨蓝文字链接**：一行小蓝字与旁边的标签、数值仍是同一副字形，能点的证据只剩色相，七八处动作挤在一起时更是谁也认不出谁——v0.34 治「灰字分不出能点」用的药（换颜色）没治到病根（没有控件形状）。改立两级：①**区块级动作**（上游设置、编辑 / 完成、管理、检测、批量粘贴 / 改为单条）走 `.act`——26px 描边小按钮，14px 笔画图标 + 文字，静止透明底、悬停 `--sunken` 加深描边；v0.34 ② 当年反对描边按钮的理由（「一排同等分量的描边按钮会盖过行首正主」）用**位置**解，不用形状解：这一级全部上移到区块标题行（「管理」「检测」从凭证行移到「上游凭证 · N」标题旁，凭证行只剩掩码 + 图标微钮 + 「共 N 把在轮」），标题行本来就是动作的家，不与数值抢行。②**值旁动作**（显示 / 隐藏、复制、删行）走 `.act-icon`——26×26 无边框纯图标微钮（aria-label + title 补名），贴着值站、分量压到最低；复制成功原地变 ✓ 绿一拍。图标新开 `icons/acts.tsx`：统一 16 视口、1.5px 圆头笔画、currentColor 随文字变色，与 §6 供应商标识（彩色资产转灰阶）是两套逻辑互不掺和，同一行里不混第二种笔画粗细。**上限（v0.36）连带换装成芯片族**：未设 = 虚线胶囊「+ 上限」，已设 = 实底芯片「上限 ~N」悬停浮出铅笔，编辑 = 胶囊输入组（数字与「token」单位同框，聚焦整框点亮 `--accent`，全局 `input:focus` 的内描边在框内明确关掉，支持 `200k` / `1m` 缩写）——三态同一副胶囊身形，状态切换不再是「一行字突变成输入框」。**不跟的**：「详情」仍是文字链接——它展开一段内容不执行动作，白名单②本来的辖区；「添加」仍 `btn-quiet`；删除仍 ghost 起步（理由同 v0.34：举起要变实心红）。`.btn-link` 类删除（用户清零），SecretValue 换装随组件走遍全站 |
| v0.37 | 2026-08-28 | jinpenga | **左栏底部加「导入配置」**（口径层 v1.00 / [#59](https://github.com/SimonGino/portage/issues/59) 的页面落点）。与「导出配置」并排、同款素按钮，理由同款：它动的是**全部**业务配置，挂在任何一页下面都会读成「只导这一页的东西」。流程三步：选文件（`.yaml`/`.yml`）→ 弹框覆盖警告——正文写明「文件里没有的渠道、接入点、API Key 会被删除」，最终后果落在 `Confirm` 确认键上（「确定覆盖当前配置？」，沿 v0.35 ② 「提示长在人下一步必按的那个键上」的成例）→ 提交后同弹框给变更清单（`.import-changes`，每行一条「新增渠道 xxx」式短句；无变化明说「配置无变化」）。**失败的 400 报文以 pre-wrap 原样显示**（新增 `.bar-pre` 修饰类，不动 `ErrorBar` 全站默认）：后端是「一次报全」的多行原文、每行一条问题，揉成一段就没法逐条对着改。**成功关框后整页重载**：整份配置换掉了，各页攒着的本地状态全部过期，重载比逐页打补丁诚实 |
| v0.36 | 2026-08-28 | jinpenga | **模型行加「输入上限（估算）」**（§5.1；口径层 v0.99 的页面落点）。与协议子集同层同形：未设时整格只是一个「上限」墨蓝文字动作（§4 白名单②，不给空壳「0」占位），设了显示「上限 ~N」；点开是行内小输入框，失焦或回车即存，空/0 = 清成不限。文案一律带「估算」——判据是字节 ÷4 不是真分词，界面不许把它说成精确值 |
| v0.35 | 2026-08-28 | jinpenga | **两区块回到常驻展开 + 最后一把启用凭证删得掉**（§5.1；PO 2026-08-28 裁决，删除放行的机制经「连带停用渠道 vs 放宽校验」二选一拍板取前者）。①**推翻 v0.34 ① 的默认收起**：「API 地址」「上游凭证」回到常驻展开——管理、检测、每协议的预览地址都要一眼可见一击可点，折一层就是多一步；v0.34 立论的「第一眼该是纳管模型」让位于「常用动作不该多一步」，v0.94「不该藏两层」与 v0.33 的次序（地址在上）不动，`DetailBlock` 退回标题壳（尖角、摘要、defaultOpen 一并拆除，新增标题旁 action 位）。**API 地址常驻的是只读预览**（PO 同日指着截图补充裁定「只要默认预览地址就 OK，其他收到编辑里面」）：默认每协议一行完整预览地址，输入框、删行、「添加端点」收进标题旁「编辑」原地切换（机制经「行内切换 vs 收进上游设置弹框」拍板取前者——收进弹框会把地址重新藏回两层，推翻 v0.94 提出来的理由），「完成」只收面板不提交，每一笔仍在失焦时写掉。②**删除（或停用）启用渠道的最后一把启用凭证不再被写后校验拦死**：key 失效了必须删得掉，但「能保存的配置一定能启动」的校验（`store.Validate`）不放宽——出路在前端：把后果写进确认键（「删除并停用渠道？」/「连渠道一起停用？」，`Confirm` 新增 `confirm` 文案参数），确认后**先停用渠道再动凭证**，两笔各自成事务。渠道被启用中的接入点引用时第一笔仍会被校验打回，错误原文已写明「接入点要跟着停用」，照实回显不另包装 |
| v0.34 | 2026-08-26 | jinpenga | **「API 地址」「上游凭证」两区块默认收起 + 行内文字动作走墨蓝链接**（§5.1；PO 2026-08-26 指着截图两问，逐项裁定「都同意」）。①**常驻改默认收起**：收成标题行 + 尖角 + 一句摘要（地址摆已声明的协议，凭证摆「共 N 把在轮」——收起不该把「里面有什么」一并藏掉），点标题行原地展开。**推翻的是 v0.94 / v0.33 的「常驻」，但 v0.94「接一家上游要填的全部，不该藏两层」的理由保留**：不塞回「上游设置」弹框，常驻只是降级成一击可达。立论就是 §5.1 自己那句「主画布第一眼是这个渠道的纳管模型」——两块常驻把模型列表推到一屏以下，日常进这页是看模型，不是改地址。**渠道缺可用凭证时凭证块默认展开**：警告条「在下面『上游凭证』贴一份即可」指着它，收着就是把出口藏起来。②**行内文字动作统一走墨蓝文字链接**（新立 `.btn-link`：默认无下划线、悬停才加，同「详情」那套）：上游设置、删行、显示 / 隐藏、复制、管理、检测、批量粘贴。此前它们是灰字 ghost / quiet，静止时无边框无底色，与「协议」「API 地址」这类标签同一副长相，能点与不能点分不出来。**这是 §4 白名单②既有条款的适用面扩大，不是新开落点**（白名单②立的就是「链接说『这段字能点』」）；都是小字动作，5% 面积上限不受影响。显示 / 复制挂在 `SecretValue` 上，API Key 页同步换装——同一个组件不该在两页长两样。**不跟的**：「添加」这类提交动作仍 `btn-quiet`（它是按钮不是链接），删除（`Confirm`）仍 ghost 起步——它举起后要变实心红，起步就墨蓝会让红色那一下失去对照。`.btn-text` 类随之删除（唯一用户「上游设置」已换装） |
| v0.33 | 2026-08-24 | jinpenga | **「上游设置」井改弹框，「API 地址」「上游凭证」两常驻区块对调**（§2 表面清单、§5.1；PO 2026-08-24 指着截图「交互有点烦」两问逐项裁定）。①**推翻的是什么**：口径层 v0.75 立的「上游设置」展开井——v0.94 把凭证与 base_url 提出去之后，井里只剩改名、并发、能力位、删除，机制（展开顶开页面、独立保存按钮、收起后靠小圆点提醒未保存）比内容重；且这一页别的次级操作（管理、检测、挑选）全是弹框，井是仅存的第二种形制。改成条内按钮点开 Dialog：标题归框头，保存沉右下实心墨（同检测弹层的主按钮位），删除渠道 ghost 起步坐保存对面（`Confirm` 因此补了 `type="button"`——它头一次站进表单里）。未保存改动喂 Dialog 的 guard（遮罩误点不关、Esc 照关即丢弃，Dialog 既有语义），井时代的 wellMounted 保挂载与小圆点机制整套拆除。**§2 表面清单第③项没删**：展开井这种表面还剩排行页切片井一个实例，删的是「上游设置」这个用例。②**两常驻区块对调，地址在上**（推翻 v0.94 参照 Cherry Studio 的 key 在上）：地址定义这个渠道是谁、声明了哪些协议（口径层 v0.96 ②「填了即声明」之后更是如此），凭证是从属物；接一家上游的顺序也是先填地址再贴 key。**这不是口径层变更需重开的事**——功能、字段语义一件没动，口径层 §2.2 那句「点开在列表上方展开 sunken 井」属页面形制描述，随本条同批订正（口径层 v0.97 只做回写，不另立论） |
| v0.32 | 2026-08-22 | jinpenga | **检测改弹层、API 地址改每协议一行**（§5.1；口径层 v0.96，PO 2026-08-22 拿 Cherry Studio 三张截图——密钥管理弹框、检测按钮、添加自定义提供商表单——裁定采其交互）。①凭证行「检测」不再直接在页面上铺探测结果，点开检测弹层：预选当前凭证、选全部或单个模型、勾协议（默认 openai），结果在弹层内显示为模型 × 协议矩阵，关弹层即失；「管理」弹框每把凭证行加检测动作，开同一个弹层、预选那一把。夹在凭证与模型列表之间的常驻探测区块（探测行 + 模型矩阵）随口径层子路径探测层一起拆除——v0.46「结论离被评价的列表越近越好」对常驻结果不再适用，检测自此是一次性采样，发起、勾选、结果同一个容器。模型行不放检测按钮（口径层 v0.96 ③：弹层单选已覆盖）。②API 地址区块从单行改每协议一行：协议名 + 地址失焦即存、预览逐行跟随（§7「能拼出来的结果就摆出来」原样成立），「添加端点」加行、删行 = 取消声明、至少保留一行、不加确认。③新建表单端点设置照参照图：OpenAI / Anthropic 常驻、OpenAI Responses 收「更多设置」——回退序里它排第二但用得最少，折叠的是使用频率不是优先级。视觉与命名仍不跟参照（v0.28 羊皮纸、「上游凭证」命名不动，同 v0.94 ② 的边界） |
| v0.31 | 2026-08-20 | jinpenga | **详情框加「排队」一格**（[#7](https://github.com/SimonGino/portage/issues/7)，照 v0.22 的三条成例落）：①**框里每一段各带各的判据**——排队耗时只在真排过队（`queue_wait_ms > 0`）时摆。这一列写侧每行必落且不可空（口径层 v0.52：「没排队」与「排了 0ms」同档），照挂在所有行上会让人读出「这次排了 0ms」，而绝大多数行的实情是压根没进闸。**这一格与 request-id 的空串处理刻意不同**（那边空串摆「—」占位）：id 那格答的是「有没有可对账的凭据」，缺了也是答案；排队那格答的是「等了多久」，没进闸的行上这个问题本身不存在，摆「—」等于宣称它该有个数。②**「详情」按钮的判据跟着扩一半**：`status >= 400 || request-id 非空 || 排队耗时 > 0`——判据仍是 v0.22 那句「这一行有没有细节可看」，排过队的成功行如今多了一段可看，不进按钮判据它就够不着（正是 #7 要治的「只写不读」，换个位置再犯一遍）。③位置挂在「状态」之后：`queue_full` / `queue_timeout` 两个错误词落在状态那格，这一格答的是同一件事的「等了多久」。**不进九列表**，理由同 v0.22 的 request-id：它是「要用时才去找」的，不是每行都要扫的 |
| v0.30 | 2026-08-18 | jinpenga | **排行页改成三层下钻，§4 为它开出唯一一个分类色盘的口子**（口径层 v0.86，PO 2026-08-18 拿一张 AI Usage 面板的三档截图作参照裁定）。①§2 那条「按天柱状图也不塞进排行」作废——去掉概览页不变，变的是时间轴的落点，它成了排行页下钻链的第一层。②§5.2 排行条目重写：节律带（24 柱 / 7×24 点阵 / 六周日历，墨色单色五档、分位切档）→ 切片井（缓存 / 净输入 / 输出）→ 跟着区间走的排行列表；未到的区间不画不可点不进分母。③**§4 头一次让颜色承载「身份」而不是「语义」**：五槽分类色盘只给排行页的环形图与占比堆叠条，色跟实体走不跟名次走，第六个起并进中性灰的「其余（N）」，每个系列必须直接标名字 + 百分比（五个色对羊皮纸底有四个不足 3:1，实测 1.96–2.9:1），不进表格数字 / 徽章 / 状态。色值以 `dataviz` 校验器在 `#f5f4ed` 上跑过的为准。**墨蓝 5% 那条自查不受影响**——分类色盘不是强调色，它不给主指标、不给按钮、不给选中态。④五个统计量那一行**不算** §9「指标堆砌成一排等大方块」：无框无底、字号有主次、五个读数出自同一个数组。⑤PO 同批裁定环形图与堆叠条照参照图留（口径层 v0.86 ⑧给「同一份数据不在两处各说一遍」开的口子——那条早年住在本文 §6 首条，现在的落点是口径层 v0.60），但收掉环底下那份重复图例 |
| v0.29 | 2026-08-18 | jinpenga | **暗色主题整个删掉，管理端只做亮色**（PO 2026-08-18 裁定）。**这是 PO 同日推翻自己上午那条裁定**：v0.28 四项裁定之二写的是「保双主题，暗色不做羊皮纸、做同一族的暖调深墨，`--accent` 提亮成 `#9db8dd` 保对比度，§1.5『亮 / 暗同等可读、跟随系统』不动」，而 §1.5 是从初稿起就在的口径，本次一并作废。**§1.5 是改写不是删掉**——改成「只做亮色：暖底羊皮纸是唯一一套表面，不跟随系统深浅色，也不做切换器」。「只做亮色」得有个正文落点，只记在版本记录里等于正文没说；序号也就不动，全文对 §1.4 的引用（§4、`styles.css`、`ui.tsx`、`icons/index.tsx`）不受影响。**连带作废的是它的立论**：v0.28 特意记过「当初立它的理由（`web/src/styles.css` 开头那条注记：『这东西多半开在一堆终端旁边』）仍成立」——如今依据本身被裁掉，那句注记同批改成「只做亮色……不跟随系统深浅色」。**落地逐条**：①`styles.css` 里 `@media (prefers-color-scheme: dark)` 两块（token 全套、图标显隐）删净，`color-scheme: light dark` 改 `light`；②§4「唯一主强调色是墨蓝 `#1B365D`」后面那句「（暗底提亮成 `#9db8dd` 保对比度）」删掉，暗色色值一个不留；③**20 份 `*.dark.svg` 与 `icons/index.tsx` 里 `DARK` 那套机制一并摘掉**——它们只在 `prefers-color-scheme: dark` 下才显示，暗色一走就是永远不会渲染的死资产，却照样打进 bundle（管理端 embed 进单二进制，`icons/README.md` 那条「超过 16 KB 的丢掉」算的就是这笔账）。**这不是顺手清理，是同一条裁决的必然结果**：那套机制的全部存在理由就是暗色。`LIGHT` / `DARK` 两张表收成一张 `ICONS`，`Avatar` 只渲染一份标记，CSS 的 `.avatar-light` / `.avatar-dark` 连同解释注释一并删，`icons/README.md` 两处（「深色版只有少数几个有」、「有 `dark/<name>.svg` 就一并复制」）跟着改。④**favicon 的暗色保留**：`web/public/portage-mark-v2.svg` 里的 `prefers-color-scheme` 一字不动——它是浏览器单独加载的独立文档，摆在**标签栏**上，标签栏该不该是暗的跟管理端页面用不用暗色主题是两件事，`#1c1a15` 那枚摆到暗色标签栏上就是一团黑。§11 同批写明这层分工，免得下次照着「全仓只该剩亮色」把它也扫了。⑤`design-demos/parchment/SPEC.md` 是这轮的规格源、已进仓，**不删只标**：§1 裁定表第 2 行与 §2.2 暗色 token 表各加一句作废标注（写明被本条推翻、日期与依据），历史留着。**§10 顺延要记一笔**：原第 5 条「暗色主题下表格、语义色、图标是否同等可读？」删掉，原 6 / 7 顺延为 5 / 6——**v0.28 正文里「并进 §10 自查第 7 条」（指墨蓝面积那条）今为第 6 条**，版本记录是历史不回改，指向记在这里。**壳、路由、组件结构、术语、Go 后端一件未动**；也不是口径层变更，`docs/口径层设计.md` 不跟着改 |
| v0.28 | 2026-08-18 | jinpenga | **表面整体换血为羊皮纸方案**（PO 2026-08-18 给六条设计取向：暖羊皮纸底 `#f5f4ed`、唯一主强调色墨蓝 `#1B365D` 且原则上不超页面 5%、灰阶全部带黄褐暖调拒绝常见 SaaS 冷蓝灰、层级主要靠衬线与字号间距而非彩色卡片图标、禁硬阴影 / 渐变 / 3D / 装饰性图表、深度靠边框与极弱阴影与留白；同日就四个岔口逐项裁定。hex 与职责清单的唯一事实源是 `design-demos/parchment/SPEC.md`）。**换的是表面不是壳**：§2 的结构条款（248px 左栏、五项导航、模型页主从、展开井默认收起，口径层 v0.75）一字不动，本次不重开；§5 五页各答一问、§6 两套图标逻辑、§7 表格与表单、§8 动效同样不动，改动限于色彩、字体、边框、圆角、留白。**被推翻的逐条**：①**纸灰底 `#f3f3f1` → 暖羊皮纸**（左栏与页面底 `#edece3`、主画布 `#f5f4ed`）。v0.23 那个冷纸灰是「白卡片浮在纸灰上」的地板，它的全部作用是把纯白画布衬起来；画布一旦不再是纯白，它就只剩一层偏冷的脏灰，没有第二重身份。②**「不再另开一抹 accent 去兼四件事」（v0.24 收进正文的那句）→ 引入墨蓝**，白名单三处：焦点环 / 文字链接 / 页面级合计与主指标那一个数。**与 v0.4 那次的区别**：v0.4 治的病是同一抹蓝同时兼主按钮、导航选中、开关打开、芯片选中，四个位置上各代表一种**状态**，于是「蓝 = 什么」答不出来，§1.4 落空；本次墨蓝**只兼三件、且三件都不是状态**——焦点环说「键盘现在在这」、链接说「这段字能点」、主指标说「这一页只有这一个数要先读」，三者是同一个意思（注意力落点）的三种形态，主按钮与导航选中态明确留在实心墨色与 `--sunken` + 字重。**也就是说 v0.4 立的判据没被推翻，被推翻的只是 v0.24 那句结论**：那句立在「单色够用」这个前提上，而纸媒化之后整页只剩暖灰四档，墨色描边的焦点环落在暖灰底上看不出是个环，前提没了。为防它重新长回四件事，正文同时写死一条可执行的上限——**墨蓝面积不超过页面 5%**，并进 §10 自查第 7 条。③**Outfit display → 衬线标题**（`Source Serif 4`；中文落系统宋体，不加载中文衬线 webfont）。④**中性灰 → 暖灰**：底、线、井、文字四档同族带黄褐调，语义色四套与图表数据色（`--data-in` / `--data-out`）一并按暖基底重调——冷调那套摆到羊皮纸上会发紫。⑤**圆角 20 / 16 → 10 / 8**（页头控件与按钮 12 → 6）：大圆角是「白卡片浮在纸灰上」那套语言的一部分，纸媒化之后不成立。**四项裁定逐项记**：①**壳不动，只换表面**（见上）；②**保双主题**，暗色不做羊皮纸、做同一族的暖调深墨，`--accent` 提亮成 `#9db8dd` 保对比度，§1.5「亮 / 暗同等可读、跟随系统」不动——当初立它的理由（`web/src/styles.css` 开头那条注记：「这东西多半开在一堆终端旁边」）仍成立；③**衬线只给标题**（页标题 / 区块标题 / 渠道身份条上的渠道名 / 空态首句），表格 / 表单 / 按钮 / 导航一律无衬线，理由是同一件事的两头：中文衬线 webfont 是几 MB，不加载就意味着中文衬线只能落各机自带的宋体、逐机不一致，而九列中文流水表在最小那档字号上最赌不起可读性；④**墨蓝三处白名单**，明确不给主按钮、导航选中、开关、Segmented、页码当前页、任何徽章胶囊，表内普通数字不整列刷蓝。**参考图里的 `ROLE` / `ACTIONS` 全大写小标签不引进**，§9「全大写小标题」那条禁令一字不动——中文界面本来也没有大写，为了像参考图而造一种界面里不存在的字形是纯亏。**§10 第 6 条与本次「深度靠边框」不冲突**，判据同 v0.16：那一条禁的是围合出一个**面**并靠它宣布范围，本次的边框画在真实分组处（表面之间的界、段标题下的细线），不围合、不带底、不产生嵌套；该条同批加一句括注写明这层意思。**这不是口径层变更**——功能范围、信息架构、字段语义、术语一件未动，`docs/口径层设计.md` 不跟着改（已核：该文件正文不含任何具体色值与字体名）。同批补齐**「信息」那一档语义色**：§4「语义色仅四个」一直这么写，CSS 里却只落了 ok / warn / err 三套，本次把 info 补上——**它与强调色同源**（这套暖底上唯一的蓝就是墨蓝，信息没有理由再发明第二抹，两者靠底色、描边与文案区分），目前尚无消费者，先把档位定下来。品牌标记换色见 §11 |
| v0.27 | 2026-08-17 | jinpenga | **「端点」列的副行由上游协议换成出站路径**（就 [#20](https://github.com/SimonGino/portage/issues/20) PO 2026-08-17 裁定「显示真实的入站 endpoint 与出站 endpoint，不再显示协议」，口径层 v0.83）。①**这是 v0.26 那条裁决走完的另一半**：v0.26 让主行从入站协议换成了入站路径，理由是「协议答不了这行是 messages 还是 count_tokens」；副行的上游协议有同一个毛病——它答不了「转成了哪条路径」。而且这个方向上协议更靠不住：入站协议由路径唯一决定（反推是恒等的），出站协议**推不出**出站路径，`count_tokens` 透传时上游协议是 anthropic，照 `UpstreamEndpoint` 反推会把它说成 `/v1/messages`。②**协议不再出现在这一列**，两格都是路径，去 `/v1/` 前缀、全路径进 `title`，副行带箭头表方向。协议对没有丢——详情框里「协议」那一格原样在（那里不受宽度约束，v0.26 分格时就是为了这个）。③**只摆路径不摆 base_url**：CLAUDE.md 那条硬约束（错误回显与日志严禁泄露上游 key 与 base_url）在页面上同样成立，「打到哪家」看渠道名。④**回落两档，靠入站那格分辨**：两格皆空 = 加列前的老流水，整格退回「入站 / 上游 <协议>」的原显示（同 v0.26 的理由：那些行确实没存过路径，摆缺值符号等于把已知的协议也扔了）；入站有值而出站空 = 这次请求**根本没发到上游**（401 / 429 / count_tokens 撞非 anthropic 渠道的 501 / 并发闸队满），副行给「—」。详情框里两格皆空时整格塌成一个「—」而不是「— → —」——摆两个破折号只是把同一个缺值说了两遍。⑤**筛选不动**：仍只有入站端点那个 Picker，出站只展示不筛（问「这批 count_tokens 怎么样」用的是客户端打的那条路径）。九列不变 |
| v0.26 | 2026-08-17 | jinpenga | 不新增条款，记一条 PO 裁决（就 [#17](https://github.com/SimonGino/portage/issues/17) 「端点」列的三档摆法裁「路径顶掉入站协议」，口径层 v0.82）：这一列**此前名不副实**——列头写着「端点」，摆的是入站/上游协议对。真端点进来之后主行换成端点路径（去 `/v1/` 前缀省宽，全路径进 `title`），副行 `上游 <协议>` 一字不动。**顶掉入站协议不是省版面，是去重**：入口协议由路径决定、不嗅探请求体（`/v1/messages` → anthropic 是恒等的），两个都摆是同一件事说两遍；而路径能答的那个问题（这行是 `messages` 还是 `count_tokens`）入站协议答不了，两者的 `client_protocol` 同为 anthropic，这正是本票的病根。**老流水退回原显示**（`endpoint` 空串）而不是摆「—」：那些行确实没存过路径，摆缺值符号等于把它们已知的入站协议也一并扔了。**详情框里两者分成两格**（「端点」「协议」）——框里没有表的宽度约束，而这本来就是两个问题；v0.19 给这个框定的「顶上带一小块关键信息（模型 / 端点 / 状态）」原样成立，只是「端点」那一格从此名副其实。**筛选另起一个 Picker** 而不是并进「只看失败」那个 Segmented：连「全部」一共 5 段，横排比模型下拉还宽，而这一排右边还有两个控件（§4「一屏之内看得完」）；控件走 Picker 不走原生 `select`，同 v0.19 给模型筛选定的那条。九列不变——这一批没有新增列 |
| v0.25 | 2026-08-17 | jinpenga | **推翻 v0.24 / A-v2「动词标题 + 一句 lede」**（口径层 v0.76）。PO 2026-08-17 看落地页口头裁决：页标题与导航同名（栏目名），不加副标题，大字也不再口语化。页标题从 40px 收到 26px，页头控件统一 36px。模型页渠道 / 模型分层：渠道是 sunken 身份条（Outfit + 小字「渠道」），模型是等宽列表（小字「纳管模型 · N」）；获取模型列表与手动添加收成页头组合按钮，添加条默认收起。主语没变（仍是纳管模型），变的是标题文案和层级 |
| v0.24 | 2026-08-17 | jinpenga | **把 A-v2 验收结果收成现行正文**（口径层 v0.75）。正文不再以 Geist 顶栏 + 居中 1520 为默认、再在末尾打补丁：壳、五项导航、模型页主从、展开井、两套图标逻辑写进 §2–§6。同批记渠道 / 模型图标拆开（官方渠道有图，中转与自建首字母；模型名另表按族认，两套不许互为兜底）。历史条款仍在下表，被推翻的以本节之上为准 |
| v0.23 | 2026-08-16 | jinpenga | **推翻 v0.5 / v0.17「侧栏不做」**；壳改 A-v2（口径层 v0.75）。内容区不再顶栏 + 居中 1520 画布：248px 左栏（品牌 + 五项药丸选中 + 账号钉底），主画布白、大圆角 20，纸灰底 `#f3f3f1`。导航五项：模型 · API Key · 调用记录 · 接入点 · 排行；去掉概览。模型页主从：渠道列表只在左栏中段出现，主画布主语是纳管模型，「上游设置 / 上游凭证」收进展开井。字体 Outfit + IBM Plex Sans / Mono。暗色 `prefers-color-scheme` 仍在，给 A 的 token 配一套。 |
| v0.22 | 2026-08-14 | jinpenga | 不新增条款，记一条 PO 裁决（就 [portage-legacy#81](https://github.com/SimonGino/portage-legacy/issues/81) 给的三个落点选①，口径层 v0.67），**推翻 v0.19 里「判据不变」那半句**：能点开「详情」的不再只是状态码 ≥400 的行，而是**这一行有没有细节可看**（`status >= 400 || upstream_request_id !== ''`）。v0.19 写那句时这个框里只有上游原文一样东西，判据跟着内容走没有问题；上游 request-id 进来之后前提没了——它**成功行也有**，而成功行的 id 一样要拿去对账。**容器的语义本来就是「这一行的额外细节」**，上游原文只是它此前唯一的内容，v0.19 立的「几百字节自由文本不该挤进一列」与定高视口那两条一字不动。另外三条同批定下：①**框里每一段各带各的判据**——上游原文仍只在失败行摆，照挂在成功行上会显示「没有存下上游原文」那句兜底文案，而对一次 200 来说那句是假的；②id 走 `CopyCode`（§4「等宽字体只给标识符」的既有落点），对账要把它粘给上游，手抄必错；③没有 id 的行摆「—」，与表里其余缺值同一个符号，不摆空壳也不额外解释。**不给它单开一列、也不压在时间列下面**：二十几个字符的串会把这张九列表撑出卡片，而它是「要用时才去找」的东西，不是每行都要扫的（§4「一屏之内看得完」） |
| v0.21 | 2026-08-14 | jinpenga | 不新增条款，清两笔本文自己欠的账（评审 PR [portage-legacy#86](https://github.com/SimonGino/portage-legacy/pull/86) 时发现，PO 裁定一并订正）。①**术语订正**：四处「网关 key」改「API Key」、两处页名「用量查询」/「用量」改「观测三页」。`CONTEXT.md` 术语表 2026-08-12 就把「网关 key」列进 _Avoid_，本文 §2 那条「术语与术语表严格一致」的括号里却一直举着这个旧称——一条禁令拿被禁的词做例子，谁照着抄都会抄错；界面文案本身早已合规（`b5902bf` 把列头改回「API Key」是这一轮最后一处）。§2 该条同时补一句「这一条管本文自身」，把本文纳入它自己的管辖范围。②**补记 v0.14 末句作废**：那句「卡片不再与页面同名」立在「导航项仍叫用量、卡片叫概览/调用记录」这个前提上，v0.17 拆页时前提没了——三页与三张卡一一对应，同名是这次拆分的**结果**而非疏漏（口径层 v0.60 已明写「PO 裁定索性让页面跟卡片同名」，本文 v0.17 漏记，故读起来像 §2「标题不写栏目名」的违例）。§2 那条禁的是拿「数据概览」这类栏目名冒充运营问题的标题，而这三个名字各自就是那一页要答的问题，不在禁列 |
| v0.20 | 2026-08-13 | jinpenga | 不新增条款，记一条 PO 裁决（「分页按钮放在右下角，可以直接跳转那种」，口径层 v0.61），**推翻 v0.13 ②**：翻页从「上一页 / 第 N 页 / 下一页」换成能点的页码，位置从表下面**居中**改成**右下角**（v0.12 的「加载更多」居中一颗、v0.13 沿用的居中一并作废）。①**页码为什么现在诚实了**：v0.13 拒它的原话是「摆一排 1 2 3 却点不动中间那些，比只给两颗按钮更糟」，那句立在「翻页只有游标」这个前提上；口径层 v0.61 把前提换掉了（进页时定住看哪一批，之后在这批里按页取），页码是真能跳的、总页数也是真的，理由随之作废。②**为什么靠右**：这排东西现在有七八颗按钮宽，居中意味着两头都不贴任何一条边，读起来像浮在表底下；靠右之后末页那颗与标题栏那排控件、与表的右缘共用一条边界，而**页码增减时抖的是左边**——右下角这个位置本身在稳住手要去按的那几颗（与 v0.19 定高视口同一个病因）。仍在表下面而不并进标题栏：它是这张表的下一页，不是页面级动作。③**当前页复用 `btn-quiet.is-on`**（底色 + 文字色描边）而不新开一抹颜色——§3「accent 只给焦点环」，选中态在 Segmented 与筛选按钮上已经是这套语言，页码没有理由自己再发明一套。④整排钉 30px 高，与标题栏那排同尺寸（v0.19）；页码按钮 `min-width` 给到与高同宽，个位数就是正方形——宽度随位数变的话，从第 9 页翻到第 10 页整排会横着挪一格，而手正停在原来那颗上。⑤**省略号不做成按钮**：中间断掉的那几页没有一个「代表页」，能点会让人以为点了会去某处，而去哪一页说不清；只隔着一页时直接把那一页摆出来，省略号跟一个页码差不多宽，替掉一页是白换。⑥「第 N 页」那个文字状态（`.pager-at`）删除——高亮的那颗数字已经说了同一件事，§6「同一份数据不画两遍」 |
| v0.19 | 2026-08-13 | jinpenga | 不新增条款，记一条 PO 裁决（「调用记录的最好能固定高度，不然动来动去，详情什么可以弹框显示」），**推翻 v0.12 末句的前半**：失败行的上游原文不再展开成表里的第二行，改由弹框承载；同批给流水表加**定高视口**。两件事同一个病因——这张表的高度自己会变：失败行在「状态」格里多挂重试、错误词与详情按钮，量出来 107px，比成功行的 67.5px 高一半，一页里失败几条决定整块表多高，底下的翻页按钮跟着上下跑，手要追着按钮走；点开一行原文再叠一次，下面所有行连同按钮一起掉几百像素，想比对的那几行直接出视线。视口高度按一整页十行的**实测**值给（表头 40.5 + 10×67.5 = 716，行数见 v0.18），超出的部分在视口内部滚，页面本身不动；空态也放进同一个视口，否则筛成空的一瞬间这块塌掉，正是要消掉的那种跳动；矮窗口上让位给屏幕（`min(716px, 100vh - 190px)`），硬撑的结果是页面与视口两条纵向滚动条并排。表头 sticky 钉在视口顶上——视口里滚起来之后，滚掉的第一样东西不该是列名。**v0.12 立那条时的理由仍成立**（几百字节的自由文本挤进一列会把表撑变形），弹框只是把它端得更远：框浮起来就跟那一行脱开了，于是顶上还得补一小块「这是哪一条」（模型 / 端点 / 状态）。**判据不变**——能点开详情的仍是状态码 ≥400 的行而非 error 非空（口径层 v0.53），换的只是承载它的容器；§5 末条「不弹全局对话框」管的是校验失败要就地报错，与人主动点开一段长文本不是一件事。**同批**（PO 看了落地效果后指同一页的标题栏：「这个地方也调整下，不然都不协调，大小宽度等」）：区段标题栏里的 Picker / Segmented / 按钮**钉成同高同圆角**（30px / 6px）。此前三者各按各的内边距长成 27.5 / 33.5 / 31.5 三个高度、6 / 7 / 6 三个圆角——差几像素不会被读成「它们不一样高」，只会被读成「这一排没对齐」。高度写死而不是用内边距凑：三者内部结构不同（Picker 里有 16px 厂商图标、Segmented 里还套一层按钮），同一个内边距在它们身上得不到同一个高度。规则挂 `.section-head` 而不是挂 `.row-actions`，因为概览页的天数开关是单个控件直接传进去的、外面没有那一层，挂错地方的结果是同一个开关在概览与排行两页不一样高。Picker 触发器同时补一个 `min-width`（10em，上限仍 14em）：它按内容收窄，选中一个长模型名会把右边整排控件顶着走一截，与本轮定高同一个病 |
| v0.18 | 2026-08-13 | jinpenga | 不新增条款，记一条执行口径：**调用记录每页 10 条**（PO 裁定，原 50）。判据是行的实际高度而不是行数——这张表 9 列，时间之外几乎每格都还带一行副信息（上游模型、上游协议、首字耗时、缓存读写、重试与错误），一行顶普通表格行两倍多，50 行是好几屏；而这一页问的是「刚才那几次怎么样」，答案基本都在最上面几行里。翻页是游标式的（v0.13），往回翻的代价只是一次请求。§4「调用日志默认紧凑密度」管的是行内密度，跟一页装几行是两件事，两条不冲突。后端不动：默认 100、上限 500，一页要多少由前端说了算 |
| v0.17 | 2026-08-13 | jinpenga | 不新增条款，记一条**跨页**的执行口径与一处页面切分（口径层 v0.60，PO：「所以我的意思是导航多分几个啊」）。原「用量页」拆成概览 / 调用记录 / 排行三页，§6 标题随之改名。**§6 首条「同一份数据不画两遍」从页内扩到页间**：合计（调用 / 失败 / token）只在概览摆全，排行页只留 token 合计那一个数——判据不是「它是不是重复的数」，而是「它在这一页有没有第二重身份」：在排行页它是每行占比的分母，删了「57.9%」就没有出处；调用与失败在那里没有这重身份，所以不摆。**侧边栏仍不做**：v0.5 拒它的立论是「Vercel 那张图挂着约 20 个目的地才值 240px，Portage 只有 4 页」，现在六页，离那个量级还远，且横排六项在 1520 上限内一行放得下。导航顺序按「配的三项（渠道 / 接入点 / API Key）+ 看的三项（概览 / 调用记录 / 排行）」，默认落地页不变（仍是渠道）。配套一处间距：`.rank-list` 上内边距 14px → 6px，它上面从「图例那一行」换成了那条带下边框的合计 |
| v0.16 | 2026-08-13 | jinpenga | 不推翻条款，给 §3 第三条「真实分组」补两个具名实例（PO：「希望在页面上加点边框，或者能够区分布局的东西」）。给了三档选项——①两条细线 ②左栏整块凹底 ③逐区段包卡片，PO 裁 ①。**它为什么不落进 §8 第 5 条**：那一条禁的是「用边框修层级」，即围合出一个**面**并靠它宣布范围；这两条线都不围合、不带底色、不产生嵌套，只标一个起点。§3 原文本来就把「真实分组处」列为边框的合法去处，两栏之间与段标题正是分组处。v0.8 撤回的是 v0.6 那个**块级表面**（有底有框的面板），跟一条线不是同一件事，故不必重开那个口子。落地两处：①两栏之间一条 `--line` 竖线，画在**右栏的左边框**上——左栏是 sticky 且被 max-height 封了顶，边框只有那一列名字那么长，三个渠道时就是一截杵在半空的短线；②区段标题下一条 `--line-soft` 细线，比抬头（`.detail-head` / `.master-head` / 导航条）那档 `--line` 轻一格，层级才在。配套把右栏段间距从 18px 提到 28px：标题上方的空档必须明显大于标题与线之间那点距离，否则那条线看着像是给上面那段收尾的。**§9 自查第 5 条（「删掉任意一个边框，层级是否依然成立？成立就删」）在这两处不成立**——删掉之后「设置 / 上游凭证 / 模型」连着三段只剩间距，而段间距与段内间距只差十几像素，正是这次被 PO 点名的那个问题 |
| v0.15 | 2026-08-13 | jinpenga | §6 新增末条「排行是列表不是表」，用量页据此把七列汇总表换成排行列表，两张卡换序（口径层 v0.58，PO：「调用记录放到最上面，用量改成 ranking」，参照 new-api 模型广场与 OpenRouter rankings）。①**换序**：调用记录在上、排行在下。v0.14 那版按「先总后分」排，而这一页的实际用法不是从总数开始——第一眼问的是「刚才那几次怎么样」。②**表换列表**：原表七列平铺，等于宣称调用/失败/输入/输出/缓存读/缓存写一样重要；换成「名次 + 图标 + 名字 + token 合计 + 占比」，其余压成附注（失败非零才上错误色并留着「失败」二字当非颜色线索，缓存两项非零才露出）。名次按 token 排——v0.11 就记过调用次数与 token 能差一个量级。③**没画占比条**：它正是 §8 点名的「一个数值画一根柱」，且 v0.11 才刚以「画的是调用次数、而问的是 token」为由删过一条 `.bar-mini`；参照的两个排行榜页也都没有条。④**精确值进 title**：表在的时候「缩写看量级、精确值去表里看」（v0.11 定的分工）成立，表没了这句就得由 title 接住。⑤参照页上的热门榜、厂商市场份额、↑8500% 环比仍不做，理由与两处硬约束见口径层 v0.55 ①、v0.58 |
| v0.14 | 2026-08-13 | jinpenga | §6 首条补「**同一份数据不画两遍**」，用量图据此把横轴从模型换成天（口径层 v0.55 同批，PO 拿 OpenRouter/ArcReel 排行榜页作参照）。v0.11 立的那张堆叠柱状图画的是各模型的 token 总量——而它正下方那张汇总表逐行说的就是这件事，还多给了失败数与缓存两列；图当时看着有用，是因为柱子比数字快，不是因为它多说了什么。换成时间之后两者各答一问：图答「什么时候烧的、在涨还是在落」，表答「谁在烧」。颜色一个没加：柱子仍是输入/输出两段，沿用 v0.11 开的那两个数据色——参照图那种按模型分色的十几色堆叠要新开一套分类色板并给 §1.4 开口子，而七八个模型以上不悬停根本读不出哪段是谁，连输入/输出的拆分都保不住。三处配套：没有调用的那天照样占一格（缺天会让剩下的柱子挤在一起，看起来像是一直在用）、横轴标签隔着摆而柱子一根不少（密到读不出来时该少的是标签）、图例那句「该看什么」从「谁占了多少」改成「最重的一天是哪天」，另补一句「最后一根是今天，还没走完」——不说的话每次看最后一根都矮一截，会被读成在掉。同批：用量页两张卡改名「概览」「调用记录」，导航项仍叫「用量」，卡片不再与页面同名 |
| v0.13 | 2026-08-13 | jinpenga | 两条 PO 裁决，都推翻既有条款，都出自用量页。①**§5 首条的「原生控件」给下拉开例外**：全站最后一个原生 `<select>`（用量页的模型筛选）换成 `Picker`。PO 的话是「下拉不要走原生的这种，自己设计组件」。原生控件本来是拿「行为可信、不用维护」换「长得不由我」，而 `<select>` 这一换是亏的——`<option>` 里塞不进厂商图标、装不下分组与搜索（同一个控件在渠道页早就得用 `Picker`），弹层选中态那抹系统蓝还与 §3「accent 只给焦点环」是两套颜色语言，且改不了。例外只开给下拉，输入框、复选框这些仍走原生。②**流水表底部的「加载更多」换成页码**（推翻 v0.12 末句那半条）：PO 裁定「用量这个地方做分页」。位置不动（仍是表下面居中一排，不与标题栏那排页面级动作混排），形态换成 上一页 / 第 N 页 / 下一页——**没有跳页、没有总页数**，因为后端走 before 游标而非 offset（流水是时间序、新行不断插到头部，offset 翻页会让同一条在两页里各出现一次），游标能回答的只有「来路」与「下一批」。这是一次诚实的降级：摆一排 1 2 3 … 却点不动中间那些，比只给两颗按钮更糟 |
| v0.12 | 2026-08-13 | jinpenga | 不新增条款，记一条对 v0.10 ①「宽度按内容给」的续裁（PO：「名字宽度可以小点，突出的是 apikey」）：**一行里谁是主语，谁就吃剩余宽度**。凭证行原先名字 `flex:2`、掩码 `flex:3`，两段都在按比例伸缩，于是「凭证 1」这种四五个字的代号占掉近半行宽；改成名字定宽 10em、掩码 `flex:1` 吃掉剩下的——人扫这一行是在找「我贴的是哪一把」，代号只是找到之后才读的东西。v0.10 那条的立论里「值又永远不出现（只写不回读）」自 v0.47 已不成立，正是它让名字当年拿到了那份宽度。同批用量页新增两个展示形态：失败行可展开摊开上游错误原文（整行让给它，不塞进某一格——几百字节的自由文本挤进一列会把表撑变形），流水表底部「加载更多」居中一颗（它是这张表的下一页，不与标题栏那排页面级动作混排） |
| v0.11 | 2026-08-12 | jinpenga | §3 新增「图表数据色」一类（`--data-in` / `--data-out`），用量页据此加入堆叠柱状图。PO 给了 68HUB 的两张参照图，走 /prototype 出了三版：A 照参照图（三张等大指标卡 + 环形图）、B（现有指标行 + 堆叠柱状图，不卡片化）、C（双栏排行榜，不画图）。**PO 裁定 B**——它拿到了参照图第二张的主体，只需给颜色开一个窄口子，不必推翻 §8「指标堆砌成一排等大方块」；A 要两条规矩一起破，而那个环只承载输入/输出两个数。同批落地：模型名底下的调用占比条删掉（它画的是调用次数，而柱状图画的是 token——「谁在烧」问的是后者，93 次调用烧 317 万 token、5 次调用烧 40 万，两者能差一个量级），指标行右侧的输入/输出两项收成一个合计（图与图例已经说过一遍）。变体全集在丢弃分支 `prototype/usage-layout`，main 只留裁决结果。参照图里的「费用」「剩余配额」没有落地，也不会有：本项目不做计费，没有数据源 |
| v0.10 | 2026-08-12 | jinpenga | 不新增条款，记两条执行口径（都是「API 密钥」那一段连挨两轮「太长了」「还是很丑」逼出来的）：①**宽度按内容给，不按栏宽给**——凭证名是「凭证 1」这种短标识、值又永远不出现（只写不回读），却和 base_url 一样吃满整栏，名字与右边的开关之间空出一大片；这一段（说明/列表/添加行）统一收到 42em 共用一条右边界，base_url 那段仍吃满，因为它装的是真会长到换行的东西。②**已经存在的东西不要长成待填的表单**——三个同等粗细的描边框叠在一起时，「库里已有这么一把」与「你正在贴一把新的」看着一模一样。凭证名改成 ghost 输入（平时是文字，悬停才显边框，聚焦才是输入框），添加行只剩一个描边框，两者的分工一眼可辨。同批顺手清掉的杂音：ISO 时刻只显示到天（完整值进 title）、说明从两行压到一行、按钮从右对齐的独立行并回它要提交的那个输入框旁边 |
| v0.9 | 2026-08-12 | jinpenga | 不新增条款，记两条本轮反复踩到的执行口径（口径层 v0.46 同批）：①**开关就该是开关**——启停这类立刻生效的状态，摆在它所描述对象的抬头上直接拨，不做成「表单里的复选框 + 保存按钮」；后者把一个一步动作拆成两步，还让人不确定拨完到底存没存。②**能拼出来的结果就摆出来，别只写提示**：Base URL 那个 `/v1/v1/…` 的坑，提示文案改到第三版仍然有人踩，加一行逐协议的完整地址预览之后就不用文案了。两条都是从 PO 给的参照界面里抄的，抄的是这两个动作本身，不是它的色彩与胶囊 |
| v0.8 | 2026-08-12 | jinpenga | §3 **撤回 v0.6 的「块级表面」例外，改立「主从两栏」**；渠道页据此重做（口径层 v0.45 同批收敛）。PO 裁定按 Cherry Studio 那种交互来：左边只管渠道，右边编辑、拉模型列表等等都在右边。v0.6 那条例外是为「一列长块里的三层结构」立的，两栏之后那个问题由**位置**解决——左栏是界，右栏就是这一项的地方，项不再需要自己的边框来说明自己有多宽。留着一条没有实例的例外只会在下次被援引成「可以包卡片」，所以直接撤回，`--panel` 退回「只给浮起来的东西」。落地不到一天就被取代，记下来是因为这轮真正学到的是：留白读起来别扭时，先问是不是信息架构错了，再考虑给它画边框 |
| v0.7 | 2026-08-12 | jinpenga | §3 **撤回 v0.5 的「左贴边」，内容区改回居中**，其余（1520 上限、v0.6 的块级表面）不动。PO 裁定：贴边之后整页往左歪，比居中难看。v0.5 立论里「居中会让内容左右漂」这一条经不起推——漂的是右边缘，第一列只在窗口宽过 1520 之后才动，而那一档屏幕上留白本来就多。贴边真实的代价是全部余量堆在一侧，视觉重心跟着偏 |
| v0.6 | 2026-08-12 | jinpenga | §3 开一个口子：**列表项内部还有结构时可以有块级表面**，渠道块据此做成带头带的面板。起因是 PO 拿两张客户端截图（分组面板 + 彩色图标底砖 + 彩色胶囊）问观感，同时提「换 Vercel 色系」。色系那条不成立——亮色本来就是白底加灰细线，跟 Vercel 同构，换 hex 屏幕上看不出差别；两张参考图好看的原因是每组内容有面板兜住，是结构不是色相。**只放这一条，彩色图标底砖与彩色胶囊两条禁令守住**：它们跟「颜色只承载语义」是同一条，破了之后状态色不再指状态。同批把渠道行头的动作串从行尾挪到渠道名右边（`.channel-head` 去掉 `space-between`），不新增条款——理由就是 v0.3 把模型压成网格那一条：名字在最左、控件在最右，中间隔着一千多像素，眼睛连不起来。同一个毛病此前只在模型那层修过，渠道这层漏了 |
| v0.5 | 2026-08-12 | jinpenga | §3 补「内容区左贴边」。起因是 PO 拿 Vercel 控制台截图问侧边栏有没有参考意义：侧边栏不取（Vercel 那张图挂着约 20 个目的地、且恰好是空状态屏，整屏都是壳；Portage 只有 4 页，240px 换四个词纯亏，窄窗口还会把七列流水表重新挤出可视区，即 issue portage-legacy#29 那个坑），但那张图里真正起作用的是内容区起点钉死不动——那是贴边给的，跟导航形态无关。1520 上限保留：贴边解决起点，上限管终点 |
| v0.4 | 2026-08-12 | jinpenga | §3 补「accent 只给焦点环」。起因是照本规范回归四页布局（去卡片化、放开内容区宽度、指标条合并、拨杆改状态文字）时发现：同一抹蓝同时在主按钮、导航选中、开关打开、芯片选中上出现，§1.4「颜色只承载语义」实际上没有落地。其余改动都是在执行既有条款，不新增口径，故不单列 |
| v0.3 | 2026-08-12 | jinpenga | §5 补「批量要先划范围」：拉模型列表那条结论条原先摆一份截断到 12 条的名单加一颗「把这 234 个加进来」，实测在真实上游（236 条）下等于劝人给自己造一份两百行的假配置。改成搜索 + 前缀分组 + 逐条/逐组勾选的挑选面板，全选只作用于当前筛出来的那些。口径不变——拉回来的列表仍是填表助手（口径层 v0.40），面板里照旧不出现「同步」、不按上游删已纳管的 |
| v0.2 | 2026-08-12 | jinpenga | 新增品牌标记：三个候选里定「hull off the water」，依据 = 唯一「缺口是主语」的读法且没有通用图标同形物（另两个分别撞 trending-up 与 stairs），16px 栅格实测也最实。同时把两份资产的分工定死——页面内 `currentColor`、favicon 写死亮暗两套色，因为 favicon 是浏览器单独加载的独立文档，没有上下文可继承。适用范围一行随之从「管视觉与交互原则」扩到含品牌标记 |
| v0.1 | 2026-08-05 | jinpenga | 初稿：由 Vercel 品牌规范骨架改写为本项目管理端设计规范；剥离 Vercel 品牌资产与报告叙事结构，保留克制内核（单色/排版先行/表格即证据/默认静止），并与安全口径（凭证脱敏）、术语表（CONTEXT.md）、v0.12 纳管口径挂钩 |
