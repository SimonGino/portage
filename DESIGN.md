# Portage 管理端设计规范

> 适用范围：M3 React 管理端。现行壳是 **A-v2**（PO 2026-08-16 验收，口径层 v0.75）。本文管视觉、交互原则、信息架构的页面级落点和品牌标记；功能范围与字段语义以 `docs/口径层设计.md`、`docs/MVP设计草案.md` 为准。
>
> 这不是聊天产品。不搬对话框、右对齐气泡、底栏输入槽。

## 0. 一句话定位

这是唯一管理员每天要用的运营工具：**每屏回答一个运营问题，层级靠排版，颜色只给状态，装饰一律不做。**

## 1. 原则（冲突时按序让步）

1. 事实与安全约束优先：凭证脱敏、错误信息不泄露上游 key / base_url，任何视觉方案不得违反。
2. 高频操作的效率优先于观感：表单少一次点击胜过任何视觉打磨。
3. 层级靠排版（字号 / 字重 / 间距 / 对齐）建立，不靠边框、卡片、色块修补。
4. 颜色只承载语义；单色是默认态。
5. 亮 / 暗双主题同等可读，跟随系统，不做可见切换器。

## 2. 壳与导航

- **248px 左栏 + 白画布**。纸灰底 `#f3f3f1`，主画布白、圆角 20。账号（改密码 / 退出）钉左栏底。推翻 v0.5 / v0.17「侧栏不做」与「内容区居中 1520」。
- **五项**，顺序即优先级，默认落地「模型」：模型 · API Key · 调用记录 · 接入点 · 排行。导航选中用浅底药丸 + 字重，不用下划线、不用蓝。
- **去掉概览**。按天柱状图不再挂在导航上，也不塞进排行。`/overview` 与 `/usage` 重定向到 `/rankings`。
- 路由：模型页仍是 `/admin/channels`、`/admin/channels/:id`（文案写「模型」）。
- 左栏中段只在模型页出现（渠道搜索 + 列表 + 新建）。其它页中段空着，不要硬塞「会话式」次级列表。
- 每页主区：页标题与导航同名（栏目名），不加副标题；页头右是主按钮或工具条，控件同高。

## 3. 排版

- 字体：Outfit 标题，IBM Plex Sans 正文，**IBM Plex Mono 只给标识符**（API Key、模型名、限定名、base_url、状态码、时间戳、token 数）。渠道名在身份条用 Outfit——那里它是栏目身份，不是标识符。整句不用等宽。
- 字号阶梯 5 档：页标题 26px / 区块标题 / 正文 / 表格与标签 / 辅助说明。不造第 6 档。页标题不再是 40px 超大 display。
- 数字一律 tabular numerals，同列同精度；token 数、耗时不做假精度。
- 界面文案中文，结论先行；术语与 `CONTEXT.md` 一致（接入点 / 候选 / 渠道 / 纳管模型 / 凭证池 / 上游凭证 / API Key）。禁止「供应商」「网关 key」「用量」当页名。

## 4. 色彩与表面

- 设计从单色开始。语义色仅四个且必须配非颜色线索（图标 / 文字）：成功、警告、错误、信息。
- 「启用 / 停用」「权重 0 摘除」「凭证被摘（401）」是状态，用语义色 + 文字；不给普通数据上色。
- 品牌色图标一律 grayscale（与 §1.4 同一条）。
- 主按钮是实心墨色。页头控件统一 36px / 12px 圆角。模型页「获取模型列表 / 手动添加」是描边组合按钮，不是实心胶囊。次按钮幽灵。选中态用 `--sunken` + 字重，不用蓝。焦点环用墨色描边——不再另开一抹 accent 去兼四件事。
- 页面是连续画布。区块靠间距分隔。允许的表面：①主画布本身；②浮层（对话框、Picker）；③**展开井**（`--sunken`，给「上游设置 / 上游凭证」，默认收起）；④**渠道身份条**（`--sunken`，模型页，把渠道从模型列表里拆开）。
- 硬性禁止：渐变、发光、毛玻璃、纹理、装饰性阴影、图标彩色底砖、彩色侧边条、聊天气泡。

## 5. 五页各答一问

| 页 | 问 | 主语 | 主动作 |
|---|---|---|---|
| 模型 | 这个上游纳管了哪些模型？ | 纳管模型列表 | 获取模型列表、启停、复制限定名、协议子集 |
| API Key | 客户端拿哪把钥匙？ | 网关下发的 key | 新建、显示 / 复制、白名单、启停 |
| 调用记录 | 刚才那几次怎么样？ | 逐行流水 | 筛失败、按模型、按端点、详情（request-id / 失败原文） |
| 接入点 | 对外叫什么名字？ | 别名 | 新建、绑候选。直连用限定名即可，不必建 |
| 排行 | 谁在烧？ | 名次列表 | 按模型 / 按 API Key / 按上游凭证；1 / 7 / 30 天 |

### 5.1 模型页

渠道列表在左栏中段：名字 + 图标 + 「缺凭证 / 无协议 / 停用」。不重复协议集、模型数、凭证计数。

主画布第一眼是**这个渠道的纳管模型**。H1 与导航同名（「模型」），不加副标题。渠道是 sunken 身份条（Outfit + 小字「渠道」），模型是等宽列表（小字「纳管模型 · N」）。渠道名旁启停立刻生效。「上游设置」「上游凭证」是条内文字按钮，点开在列表上方展开井，默认收起。获取模型列表与手动添加收成页头组合按钮，添加条默认收起。新建渠道时主区整页是表单，没有模型列表。

模型行：供应商图标 + 裸名 + 限定名（可复制，停用的不给复制）+ 协议子集 + 启停 + 删除（二次确认）。

### 5.2 API Key / 接入点 / 调用记录 / 排行

后三项维持扁平表或排行列表，不套主从两栏。

- 调用记录：定高视口、九列、每页 10 条、右下可跳页码。详情判据是「这一行有没有细节可看」（`status >= 400 || request-id 非空`）。上游原文只在失败行。request-id 可复制，空为 —。「端点」列主行是端点路径（去 `/v1/` 前缀，全路径进 title），副行是上游协议；入站协议不单摆——它由路径完全决定（老流水没有路径才退回显示它）。筛选两个 Picker（模型、端点）+ 一个 Segmented（只看失败）。
- 排行：名次 + 图标 + 名字 + 一个主指标（token）+ 占比。只留一个合计当占比分母。不做热门榜、厂商份额、环比、费用。不给每行画占比条。
- 接入点空态两句：没渠道先去模型页；有渠道没接入点则写「不必建，客户端可填限定名」。

## 6. 图标：两套逻辑，不许互为兜底

跟 Cherry Studio 同一条：**预置官方渠道有图，自定义中转走首字母；模型名另表按族认。**

- **渠道**（`CHANNEL_HOSTS` / `CHANNEL_NAMES`）：只认官方那几家（Anthropic / OpenAI / 百炼 / Vertex / DeepSeek / Kimi / 智谱等）。host 写死官方后缀，不写会把 PAI 认成百炼的宽模式。名字只认人明显按官方起的（`ali-prod`、`kimi`）。对不上就是首字母。
- **模型**（`MODEL_PATTERNS`）：只看模型名 / 限定名，按族（`gpt` / `claude` / `qwen` / `glm` / `kimi`…）。跟挂在哪条渠道无关。
- 只有供应商一级的方块，不做逐模型图标（两套视觉会打架，还得跟着上游新模型一直追）。
- 资产在 `web/src/icons/`。加图标见该目录 README。

## 7. 表格与表单

- 语义化 `<table>`。文本列左对齐，数值列右对齐。调用日志紧凑密度，配置类普通密度。
- 凭证默认掩码，给「显示」「复制」。列表与错误回显不出现完整上游凭证 / base_url（转发链路仍永远不带；管理端凭证池按口径层 v0.47 可回读）。
- 被摘除的 key、weight=0 的候选、停用的渠道：整行降灰 + 状态标注，不隐藏。
- 下拉一律 `Picker`，全站不留原生 `<select>`。
- 批量要先划范围：获取模型列表的全选只作用于当前筛出来的那些。
- 开关就该是开关：启停立刻生效，不做成「复选框 + 保存」。
- 能拼出来的结果就摆出来：Base URL 下逐协议给出完整地址预览。
- 已经存在的东西不要长成待填的表单（凭证名 ghost，添加行才是描边框）。
- 一行里谁是主语，谁就吃剩余宽度。
- 校验失败就地报错并保留输入。删除渠道 / 接入点 / key 二次确认并写明影响面。

## 8. 动效

默认静止。动效只用于：保存成功、加载中、危险操作的撤销倒计时。无滚动入场、无 hover 位移。尊重 reduced-motion。

## 9. 反模式清单（出现即打回）

- 聊天气泡、底栏「Ask anything」、把探测画成助手回复。
- 全大写小标题 / 装饰性编号。
- 居中大 hero + 卡片栅格的官网式首屏。
- 指标堆砌成一排等大方块。
- 徽章 / 胶囊标注普通元数据；图标当装饰。
- 每个区块一张卡、卡里套卡、用边框修层级。
- 无意义颜色；图表凑数（一个数值画一根柱）。

## 10. 验收自查

1. 打开管理端，能否立刻看见「纳管了哪些模型」而不是一张渠道设置长表？
2. 中转渠道是不是首字母、官方渠道和 `gpt-*` / `claude-*` 是不是有图？
3. 新增一个渠道（含纳管 3 个模型 + 2 份凭证）能否 2 分钟内完成？
4. 全站搜不出一个完整上游凭证出现在不该出现的地方（列表、流水、错误回显）？
5. 暗色主题下表格、语义色、图标是否同等可读？
6. 删掉任意一个边框 / 卡片 / 颜色，层级是否依然成立？成立就删。井是例外：收起之后列表自己成立。

## 11. 品牌标记

主标记是「hull off the water」：船体离水，下面两段水道被切断，货从缺口上面过去。**缺口是主语**。24×24 网格、3px 模数、一条 path：

```
M3,6 L21,6 L17,13 L7,13 Z  M0,17 L9,17 L9,21 L0,21 Z  M15,17 L24,17 L24,21 L15,21 Z
```

§4 的硬性禁止对标记同样成立，颜色不给标记用。

**两份资产，不是一份**：

- `web/src/brand.tsx` —— 页面内用，`fill="currentColor"`，亮暗共用。
- `web/public/portage-mark.svg` —— favicon，两套颜色写进 SVG 自己的 `<style>`，由 `prefers-color-scheme` 挑。

两处 path 必须逐字一致。favicon 路径写死在 `/admin` 下；改标记要顺手改文件名（`serveSPA` 对非 html 发一年 immutable）。

**16×16 是硬底线**。未做：wordmark；32px PNG 兜底。

资产出自 Claude Design 项目「Portage Logo Design Brief」（issue #46），不含任何第三方品牌资产。

## 版本记录

正文以本节之上为准。下面只记口径变化与依据，不记流水账。修改人一律 `jinpenga`。

| 版本 | 日期 | 修改人 | 说明 |
|---|---|---|---|
| v0.26 | 2026-08-17 | jinpenga | 不新增条款，记一条 PO 裁决（就 [#17](https://github.com/SimonGino/portage/issues/17) 「端点」列的三档摆法裁「路径顶掉入站协议」，口径层 v0.82）：这一列**此前名不副实**——列头写着「端点」，摆的是入站/上游协议对。真端点进来之后主行换成端点路径（去 `/v1/` 前缀省宽，全路径进 `title`），副行 `上游 <协议>` 一字不动。**顶掉入站协议不是省版面，是去重**：入口协议由路径决定、不嗅探请求体（`/v1/messages` → anthropic 是恒等的），两个都摆是同一件事说两遍；而路径能答的那个问题（这行是 `messages` 还是 `count_tokens`）入站协议答不了，两者的 `client_protocol` 同为 anthropic，这正是本票的病根。**老流水退回原显示**（`endpoint` 空串）而不是摆「—」：那些行确实没存过路径，摆缺值符号等于把它们已知的入站协议也一并扔了。**详情框里两者分成两格**（「端点」「协议」）——框里没有表的宽度约束，而这本来就是两个问题；v0.19 给这个框定的「顶上带一小块关键信息（模型 / 端点 / 状态）」原样成立，只是「端点」那一格从此名副其实。**筛选另起一个 Picker** 而不是并进「只看失败」那个 Segmented：连「全部」一共 5 段，横排比模型下拉还宽，而这一排右边还有两个控件（§4「一屏之内看得完」）；控件走 Picker 不走原生 `select`，同 v0.19 给模型筛选定的那条。九列不变——这一批没有新增列 |
| v0.25 | 2026-08-17 | jinpenga | **推翻 v0.24 / A-v2「动词标题 + 一句 lede」**（口径层 v0.76）。PO 2026-08-17 看落地页口头裁决：页标题与导航同名（栏目名），不加副标题，大字也不再口语化。页标题从 40px 收到 26px，页头控件统一 36px。模型页渠道 / 模型分层：渠道是 sunken 身份条（Outfit + 小字「渠道」），模型是等宽列表（小字「纳管模型 · N」）；获取模型列表与手动添加收成页头组合按钮，添加条默认收起。主语没变（仍是纳管模型），变的是标题文案和层级 |
| v0.24 | 2026-08-17 | jinpenga | **把 A-v2 验收结果收成现行正文**（口径层 v0.75）。正文不再以 Geist 顶栏 + 居中 1520 为默认、再在末尾打补丁：壳、五项导航、模型页主从、展开井、两套图标逻辑写进 §2–§6。同批记渠道 / 模型图标拆开（官方渠道有图，中转与自建首字母；模型名另表按族认，两套不许互为兜底）。历史条款仍在下表，被推翻的以本节之上为准 |
| v0.23 | 2026-08-16 | jinpenga | **推翻 v0.5 / v0.17「侧栏不做」**；壳改 A-v2（口径层 v0.75）。内容区不再顶栏 + 居中 1520 画布：248px 左栏（品牌 + 五项药丸选中 + 账号钉底），主画布白、大圆角 20，纸灰底 `#f3f3f1`。导航五项：模型 · API Key · 调用记录 · 接入点 · 排行；去掉概览。模型页主从：渠道列表只在左栏中段出现，主画布主语是纳管模型，「上游设置 / 上游凭证」收进展开井。字体 Outfit + IBM Plex Sans / Mono。暗色 `prefers-color-scheme` 仍在，给 A 的 token 配一套。 |
| v0.22 | 2026-08-14 | jinpenga | 不新增条款，记一条 PO 裁决（就 [#81](https://github.com/SimonGino/portage-legacy/issues/81) 给的三个落点选①，口径层 v0.67），**推翻 v0.19 里「判据不变」那半句**：能点开「详情」的不再只是状态码 ≥400 的行，而是**这一行有没有细节可看**（`status >= 400 || upstream_request_id !== ''`）。v0.19 写那句时这个框里只有上游原文一样东西，判据跟着内容走没有问题；上游 request-id 进来之后前提没了——它**成功行也有**，而成功行的 id 一样要拿去对账。**容器的语义本来就是「这一行的额外细节」**，上游原文只是它此前唯一的内容，v0.19 立的「几百字节自由文本不该挤进一列」与定高视口那两条一字不动。另外三条同批定下：①**框里每一段各带各的判据**——上游原文仍只在失败行摆，照挂在成功行上会显示「没有存下上游原文」那句兜底文案，而对一次 200 来说那句是假的；②id 走 `CopyCode`（§4「等宽字体只给标识符」的既有落点），对账要把它粘给上游，手抄必错；③没有 id 的行摆「—」，与表里其余缺值同一个符号，不摆空壳也不额外解释。**不给它单开一列、也不压在时间列下面**：二十几个字符的串会把这张九列表撑出卡片，而它是「要用时才去找」的东西，不是每行都要扫的（§4「一屏之内看得完」） |
| v0.21 | 2026-08-14 | jinpenga | 不新增条款，清两笔本文自己欠的账（评审 PR [#86](https://github.com/SimonGino/portage-legacy/pull/86) 时发现，PO 裁定一并订正）。①**术语订正**：四处「网关 key」改「API Key」、两处页名「用量查询」/「用量」改「观测三页」。`CONTEXT.md` 术语表 2026-08-12 就把「网关 key」列进 _Avoid_，本文 §2 那条「术语与术语表严格一致」的括号里却一直举着这个旧称——一条禁令拿被禁的词做例子，谁照着抄都会抄错；界面文案本身早已合规（`b5902bf` 把列头改回「API Key」是这一轮最后一处）。§2 该条同时补一句「这一条管本文自身」，把本文纳入它自己的管辖范围。②**补记 v0.14 末句作废**：那句「卡片不再与页面同名」立在「导航项仍叫用量、卡片叫概览/调用记录」这个前提上，v0.17 拆页时前提没了——三页与三张卡一一对应，同名是这次拆分的**结果**而非疏漏（口径层 v0.60 已明写「PO 裁定索性让页面跟卡片同名」，本文 v0.17 漏记，故读起来像 §2「标题不写栏目名」的违例）。§2 那条禁的是拿「数据概览」这类栏目名冒充运营问题的标题，而这三个名字各自就是那一页要答的问题，不在禁列 |
| v0.20 | 2026-08-13 | jinpenga | 不新增条款，记一条 PO 裁决（「分页按钮放在右下角，可以直接跳转那种」，口径层 v0.61），**推翻 v0.13 ②**：翻页从「上一页 / 第 N 页 / 下一页」换成能点的页码，位置从表下面**居中**改成**右下角**（v0.12 的「加载更多」居中一颗、v0.13 沿用的居中一并作废）。①**页码为什么现在诚实了**：v0.13 拒它的原话是「摆一排 1 2 3 却点不动中间那些，比只给两颗按钮更糟」，那句立在「翻页只有游标」这个前提上；口径层 v0.61 把前提换掉了（进页时定住看哪一批，之后在这批里按页取），页码是真能跳的、总页数也是真的，理由随之作废。②**为什么靠右**：这排东西现在有七八颗按钮宽，居中意味着两头都不贴任何一条边，读起来像浮在表底下；靠右之后末页那颗与标题栏那排控件、与表的右缘共用一条边界，而**页码增减时抖的是左边**——右下角这个位置本身在稳住手要去按的那几颗（与 v0.19 定高视口同一个病因）。仍在表下面而不并进标题栏：它是这张表的下一页，不是页面级动作。③**当前页复用 `btn-quiet.is-on`**（底色 + 文字色描边）而不新开一抹颜色——§3「accent 只给焦点环」，选中态在 Segmented 与筛选按钮上已经是这套语言，页码没有理由自己再发明一套。④整排钉 30px 高，与标题栏那排同尺寸（v0.19）；页码按钮 `min-width` 给到与高同宽，个位数就是正方形——宽度随位数变的话，从第 9 页翻到第 10 页整排会横着挪一格，而手正停在原来那颗上。⑤**省略号不做成按钮**：中间断掉的那几页没有一个「代表页」，能点会让人以为点了会去某处，而去哪一页说不清；只隔着一页时直接把那一页摆出来，省略号跟一个页码差不多宽，替掉一页是白换。⑥「第 N 页」那个文字状态（`.pager-at`）删除——高亮的那颗数字已经说了同一件事，§6「同一份数据不画两遍」 |
| v0.19 | 2026-08-13 | jinpenga | 不新增条款，记一条 PO 裁决（「调用记录的最好能固定高度，不然动来动去，详情什么可以弹框显示」），**推翻 v0.12 末句的前半**：失败行的上游原文不再展开成表里的第二行，改由弹框承载；同批给流水表加**定高视口**。两件事同一个病因——这张表的高度自己会变：失败行在「状态」格里多挂重试、错误词与详情按钮，量出来 107px，比成功行的 67.5px 高一半，一页里失败几条决定整块表多高，底下的翻页按钮跟着上下跑，手要追着按钮走；点开一行原文再叠一次，下面所有行连同按钮一起掉几百像素，想比对的那几行直接出视线。视口高度按一整页十行的**实测**值给（表头 40.5 + 10×67.5 = 716，行数见 v0.18），超出的部分在视口内部滚，页面本身不动；空态也放进同一个视口，否则筛成空的一瞬间这块塌掉，正是要消掉的那种跳动；矮窗口上让位给屏幕（`min(716px, 100vh - 190px)`），硬撑的结果是页面与视口两条纵向滚动条并排。表头 sticky 钉在视口顶上——视口里滚起来之后，滚掉的第一样东西不该是列名。**v0.12 立那条时的理由仍成立**（几百字节的自由文本挤进一列会把表撑变形），弹框只是把它端得更远：框浮起来就跟那一行脱开了，于是顶上还得补一小块「这是哪一条」（模型 / 端点 / 状态）。**判据不变**——能点开详情的仍是状态码 ≥400 的行而非 error 非空（口径层 v0.53），换的只是承载它的容器；§5 末条「不弹全局对话框」管的是校验失败要就地报错，与人主动点开一段长文本不是一件事。**同批**（PO 看了落地效果后指同一页的标题栏：「这个地方也调整下，不然都不协调，大小宽度等」）：区段标题栏里的 Picker / Segmented / 按钮**钉成同高同圆角**（30px / 6px）。此前三者各按各的内边距长成 27.5 / 33.5 / 31.5 三个高度、6 / 7 / 6 三个圆角——差几像素不会被读成「它们不一样高」，只会被读成「这一排没对齐」。高度写死而不是用内边距凑：三者内部结构不同（Picker 里有 16px 厂商图标、Segmented 里还套一层按钮），同一个内边距在它们身上得不到同一个高度。规则挂 `.section-head` 而不是挂 `.row-actions`，因为概览页的天数开关是单个控件直接传进去的、外面没有那一层，挂错地方的结果是同一个开关在概览与排行两页不一样高。Picker 触发器同时补一个 `min-width`（10em，上限仍 14em）：它按内容收窄，选中一个长模型名会把右边整排控件顶着走一截，与本轮定高同一个病 |
| v0.18 | 2026-08-13 | jinpenga | 不新增条款，记一条执行口径：**调用记录每页 10 条**（PO 裁定，原 50）。判据是行的实际高度而不是行数——这张表 9 列，时间之外几乎每格都还带一行副信息（上游模型、上游协议、首字耗时、缓存读写、重试与错误），一行顶普通表格行两倍多，50 行是好几屏；而这一页问的是「刚才那几次怎么样」，答案基本都在最上面几行里。翻页是游标式的（v0.13），往回翻的代价只是一次请求。§4「调用日志默认紧凑密度」管的是行内密度，跟一页装几行是两件事，两条不冲突。后端不动：默认 100、上限 500，一页要多少由前端说了算 |
| v0.17 | 2026-08-13 | jinpenga | 不新增条款，记一条**跨页**的执行口径与一处页面切分（口径层 v0.60，PO：「所以我的意思是导航多分几个啊」）。原「用量页」拆成概览 / 调用记录 / 排行三页，§6 标题随之改名。**§6 首条「同一份数据不画两遍」从页内扩到页间**：合计（调用 / 失败 / token）只在概览摆全，排行页只留 token 合计那一个数——判据不是「它是不是重复的数」，而是「它在这一页有没有第二重身份」：在排行页它是每行占比的分母，删了「57.9%」就没有出处；调用与失败在那里没有这重身份，所以不摆。**侧边栏仍不做**：v0.5 拒它的立论是「Vercel 那张图挂着约 20 个目的地才值 240px，Portage 只有 4 页」，现在六页，离那个量级还远，且横排六项在 1520 上限内一行放得下。导航顺序按「配的三项（渠道 / 接入点 / API Key）+ 看的三项（概览 / 调用记录 / 排行）」，默认落地页不变（仍是渠道）。配套一处间距：`.rank-list` 上内边距 14px → 6px，它上面从「图例那一行」换成了那条带下边框的合计 |
| v0.16 | 2026-08-13 | jinpenga | 不推翻条款，给 §3 第三条「真实分组」补两个具名实例（PO：「希望在页面上加点边框，或者能够区分布局的东西」）。给了三档选项——①两条细线 ②左栏整块凹底 ③逐区段包卡片，PO 裁 ①。**它为什么不落进 §8 第 5 条**：那一条禁的是「用边框修层级」，即围合出一个**面**并靠它宣布范围；这两条线都不围合、不带底色、不产生嵌套，只标一个起点。§3 原文本来就把「真实分组处」列为边框的合法去处，两栏之间与段标题正是分组处。v0.8 撤回的是 v0.6 那个**块级表面**（有底有框的面板），跟一条线不是同一件事，故不必重开那个口子。落地两处：①两栏之间一条 `--line` 竖线，画在**右栏的左边框**上——左栏是 sticky 且被 max-height 封了顶，边框只有那一列名字那么长，三个渠道时就是一截杵在半空的短线；②区段标题下一条 `--line-soft` 细线，比抬头（`.detail-head` / `.master-head` / 导航条）那档 `--line` 轻一格，层级才在。配套把右栏段间距从 18px 提到 28px：标题上方的空档必须明显大于标题与线之间那点距离，否则那条线看着像是给上面那段收尾的。**§9 自查第 5 条（「删掉任意一个边框，层级是否依然成立？成立就删」）在这两处不成立**——删掉之后「设置 / 上游凭证 / 模型」连着三段只剩间距，而段间距与段内间距只差十几像素，正是这次被 PO 点名的那个问题 |
| v0.15 | 2026-08-13 | jinpenga | §6 新增末条「排行是列表不是表」，用量页据此把七列汇总表换成排行列表，两张卡换序（口径层 v0.58，PO：「调用记录放到最上面，用量改成 ranking」，参照 new-api 模型广场与 OpenRouter rankings）。①**换序**：调用记录在上、排行在下。v0.14 那版按「先总后分」排，而这一页的实际用法不是从总数开始——第一眼问的是「刚才那几次怎么样」。②**表换列表**：原表七列平铺，等于宣称调用/失败/输入/输出/缓存读/缓存写一样重要；换成「名次 + 图标 + 名字 + token 合计 + 占比」，其余压成附注（失败非零才上错误色并留着「失败」二字当非颜色线索，缓存两项非零才露出）。名次按 token 排——v0.11 就记过调用次数与 token 能差一个量级。③**没画占比条**：它正是 §8 点名的「一个数值画一根柱」，且 v0.11 才刚以「画的是调用次数、而问的是 token」为由删过一条 `.bar-mini`；参照的两个排行榜页也都没有条。④**精确值进 title**：表在的时候「缩写看量级、精确值去表里看」（v0.11 定的分工）成立，表没了这句就得由 title 接住。⑤参照页上的热门榜、厂商市场份额、↑8500% 环比仍不做，理由与两处硬约束见口径层 v0.55 ①、v0.58 |
| v0.14 | 2026-08-13 | jinpenga | §6 首条补「**同一份数据不画两遍**」，用量图据此把横轴从模型换成天（口径层 v0.55 同批，PO 拿 OpenRouter/ArcReel 排行榜页作参照）。v0.11 立的那张堆叠柱状图画的是各模型的 token 总量——而它正下方那张汇总表逐行说的就是这件事，还多给了失败数与缓存两列；图当时看着有用，是因为柱子比数字快，不是因为它多说了什么。换成时间之后两者各答一问：图答「什么时候烧的、在涨还是在落」，表答「谁在烧」。颜色一个没加：柱子仍是输入/输出两段，沿用 v0.11 开的那两个数据色——参照图那种按模型分色的十几色堆叠要新开一套分类色板并给 §1.4 开口子，而七八个模型以上不悬停根本读不出哪段是谁，连输入/输出的拆分都保不住。三处配套：没有调用的那天照样占一格（缺天会让剩下的柱子挤在一起，看起来像是一直在用）、横轴标签隔着摆而柱子一根不少（密到读不出来时该少的是标签）、图例那句「该看什么」从「谁占了多少」改成「最重的一天是哪天」，另补一句「最后一根是今天，还没走完」——不说的话每次看最后一根都矮一截，会被读成在掉。同批：用量页两张卡改名「概览」「调用记录」，导航项仍叫「用量」，卡片不再与页面同名 |
| v0.13 | 2026-08-13 | jinpenga | 两条 PO 裁决，都推翻既有条款，都出自用量页。①**§5 首条的「原生控件」给下拉开例外**：全站最后一个原生 `<select>`（用量页的模型筛选）换成 `Picker`。PO 的话是「下拉不要走原生的这种，自己设计组件」。原生控件本来是拿「行为可信、不用维护」换「长得不由我」，而 `<select>` 这一换是亏的——`<option>` 里塞不进厂商图标、装不下分组与搜索（同一个控件在渠道页早就得用 `Picker`），弹层选中态那抹系统蓝还与 §3「accent 只给焦点环」是两套颜色语言，且改不了。例外只开给下拉，输入框、复选框这些仍走原生。②**流水表底部的「加载更多」换成页码**（推翻 v0.12 末句那半条）：PO 裁定「用量这个地方做分页」。位置不动（仍是表下面居中一排，不与标题栏那排页面级动作混排），形态换成 上一页 / 第 N 页 / 下一页——**没有跳页、没有总页数**，因为后端走 before 游标而非 offset（流水是时间序、新行不断插到头部，offset 翻页会让同一条在两页里各出现一次），游标能回答的只有「来路」与「下一批」。这是一次诚实的降级：摆一排 1 2 3 … 却点不动中间那些，比只给两颗按钮更糟 |
| v0.12 | 2026-08-13 | jinpenga | 不新增条款，记一条对 v0.10 ①「宽度按内容给」的续裁（PO：「名字宽度可以小点，突出的是 apikey」）：**一行里谁是主语，谁就吃剩余宽度**。凭证行原先名字 `flex:2`、掩码 `flex:3`，两段都在按比例伸缩，于是「凭证 1」这种四五个字的代号占掉近半行宽；改成名字定宽 10em、掩码 `flex:1` 吃掉剩下的——人扫这一行是在找「我贴的是哪一把」，代号只是找到之后才读的东西。v0.10 那条的立论里「值又永远不出现（只写不回读）」自 v0.47 已不成立，正是它让名字当年拿到了那份宽度。同批用量页新增两个展示形态：失败行可展开摊开上游错误原文（整行让给它，不塞进某一格——几百字节的自由文本挤进一列会把表撑变形），流水表底部「加载更多」居中一颗（它是这张表的下一页，不与标题栏那排页面级动作混排） |
| v0.11 | 2026-08-12 | jinpenga | §3 新增「图表数据色」一类（`--data-in` / `--data-out`），用量页据此加入堆叠柱状图。PO 给了 68HUB 的两张参照图，走 /prototype 出了三版：A 照参照图（三张等大指标卡 + 环形图）、B（现有指标行 + 堆叠柱状图，不卡片化）、C（双栏排行榜，不画图）。**PO 裁定 B**——它拿到了参照图第二张的主体，只需给颜色开一个窄口子，不必推翻 §8「指标堆砌成一排等大方块」；A 要两条规矩一起破，而那个环只承载输入/输出两个数。同批落地：模型名底下的调用占比条删掉（它画的是调用次数，而柱状图画的是 token——「谁在烧」问的是后者，93 次调用烧 317 万 token、5 次调用烧 40 万，两者能差一个量级），指标行右侧的输入/输出两项收成一个合计（图与图例已经说过一遍）。变体全集在丢弃分支 `prototype/usage-layout`，main 只留裁决结果。参照图里的「费用」「剩余配额」没有落地，也不会有：本项目不做计费，没有数据源 |
| v0.10 | 2026-08-12 | jinpenga | 不新增条款，记两条执行口径（都是「API 密钥」那一段连挨两轮「太长了」「还是很丑」逼出来的）：①**宽度按内容给，不按栏宽给**——凭证名是「凭证 1」这种短标识、值又永远不出现（只写不回读），却和 base_url 一样吃满整栏，名字与右边的开关之间空出一大片；这一段（说明/列表/添加行）统一收到 42em 共用一条右边界，base_url 那段仍吃满，因为它装的是真会长到换行的东西。②**已经存在的东西不要长成待填的表单**——三个同等粗细的描边框叠在一起时，「库里已有这么一把」与「你正在贴一把新的」看着一模一样。凭证名改成 ghost 输入（平时是文字，悬停才显边框，聚焦才是输入框），添加行只剩一个描边框，两者的分工一眼可辨。同批顺手清掉的杂音：ISO 时刻只显示到天（完整值进 title）、说明从两行压到一行、按钮从右对齐的独立行并回它要提交的那个输入框旁边 |
| v0.9 | 2026-08-12 | jinpenga | 不新增条款，记两条本轮反复踩到的执行口径（口径层 v0.46 同批）：①**开关就该是开关**——启停这类立刻生效的状态，摆在它所描述对象的抬头上直接拨，不做成「表单里的复选框 + 保存按钮」；后者把一个一步动作拆成两步，还让人不确定拨完到底存没存。②**能拼出来的结果就摆出来，别只写提示**：Base URL 那个 `/v1/v1/…` 的坑，提示文案改到第三版仍然有人踩，加一行逐协议的完整地址预览之后就不用文案了。两条都是从 PO 给的参照界面里抄的，抄的是这两个动作本身，不是它的色彩与胶囊 |
| v0.8 | 2026-08-12 | jinpenga | §3 **撤回 v0.6 的「块级表面」例外，改立「主从两栏」**；渠道页据此重做（口径层 v0.45 同批收敛）。PO 裁定按 Cherry Studio 那种交互来：左边只管渠道，右边编辑、拉模型列表等等都在右边。v0.6 那条例外是为「一列长块里的三层结构」立的，两栏之后那个问题由**位置**解决——左栏是界，右栏就是这一项的地方，项不再需要自己的边框来说明自己有多宽。留着一条没有实例的例外只会在下次被援引成「可以包卡片」，所以直接撤回，`--panel` 退回「只给浮起来的东西」。落地不到一天就被取代，记下来是因为这轮真正学到的是：留白读起来别扭时，先问是不是信息架构错了，再考虑给它画边框 |
| v0.7 | 2026-08-12 | jinpenga | §3 **撤回 v0.5 的「左贴边」，内容区改回居中**，其余（1520 上限、v0.6 的块级表面）不动。PO 裁定：贴边之后整页往左歪，比居中难看。v0.5 立论里「居中会让内容左右漂」这一条经不起推——漂的是右边缘，第一列只在窗口宽过 1520 之后才动，而那一档屏幕上留白本来就多。贴边真实的代价是全部余量堆在一侧，视觉重心跟着偏 |
| v0.6 | 2026-08-12 | jinpenga | §3 开一个口子：**列表项内部还有结构时可以有块级表面**，渠道块据此做成带头带的面板。起因是 PO 拿两张客户端截图（分组面板 + 彩色图标底砖 + 彩色胶囊）问观感，同时提「换 Vercel 色系」。色系那条不成立——亮色本来就是白底加灰细线，跟 Vercel 同构，换 hex 屏幕上看不出差别；两张参考图好看的原因是每组内容有面板兜住，是结构不是色相。**只放这一条，彩色图标底砖与彩色胶囊两条禁令守住**：它们跟「颜色只承载语义」是同一条，破了之后状态色不再指状态。同批把渠道行头的动作串从行尾挪到渠道名右边（`.channel-head` 去掉 `space-between`），不新增条款——理由就是 v0.3 把模型压成网格那一条：名字在最左、控件在最右，中间隔着一千多像素，眼睛连不起来。同一个毛病此前只在模型那层修过，渠道这层漏了 |
| v0.5 | 2026-08-12 | jinpenga | §3 补「内容区左贴边」。起因是 PO 拿 Vercel 控制台截图问侧边栏有没有参考意义：侧边栏不取（Vercel 那张图挂着约 20 个目的地、且恰好是空状态屏，整屏都是壳；Portage 只有 4 页，240px 换四个词纯亏，窄窗口还会把七列流水表重新挤出可视区，即 issue #29 那个坑），但那张图里真正起作用的是内容区起点钉死不动——那是贴边给的，跟导航形态无关。1520 上限保留：贴边解决起点，上限管终点 |
| v0.4 | 2026-08-12 | jinpenga | §3 补「accent 只给焦点环」。起因是照本规范回归四页布局（去卡片化、放开内容区宽度、指标条合并、拨杆改状态文字）时发现：同一抹蓝同时在主按钮、导航选中、开关打开、芯片选中上出现，§1.4「颜色只承载语义」实际上没有落地。其余改动都是在执行既有条款，不新增口径，故不单列 |
| v0.3 | 2026-08-12 | jinpenga | §5 补「批量要先划范围」：拉模型列表那条结论条原先摆一份截断到 12 条的名单加一颗「把这 234 个加进来」，实测在真实上游（236 条）下等于劝人给自己造一份两百行的假配置。改成搜索 + 前缀分组 + 逐条/逐组勾选的挑选面板，全选只作用于当前筛出来的那些。口径不变——拉回来的列表仍是填表助手（口径层 v0.40），面板里照旧不出现「同步」、不按上游删已纳管的 |
| v0.2 | 2026-08-12 | jinpenga | 新增品牌标记：三个候选里定「hull off the water」，依据 = 唯一「缺口是主语」的读法且没有通用图标同形物（另两个分别撞 trending-up 与 stairs），16px 栅格实测也最实。同时把两份资产的分工定死——页面内 `currentColor`、favicon 写死亮暗两套色，因为 favicon 是浏览器单独加载的独立文档，没有上下文可继承。适用范围一行随之从「管视觉与交互原则」扩到含品牌标记 |
| v0.1 | 2026-08-05 | jinpenga | 初稿：由 Vercel 品牌规范骨架改写为本项目管理端设计规范；剥离 Vercel 品牌资产与报告叙事结构，保留克制内核（单色/排版先行/表格即证据/默认静止），并与安全口径（凭证脱敏）、术语表（CONTEXT.md）、v0.12 纳管口径挂钩 |
